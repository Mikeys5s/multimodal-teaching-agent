# 析知 XiZhi 前端环境变量示例
# 复制为 .env.local 后按需修改；.env* 已被 .gitignore 忽略，禁止提交真实值。

# 后端 API 前缀。生产环境前后端同源，保持 /api 即可。
VITE_API_BASE_URL=/api

# 仅开发期使用：vite dev server 将 /api 代理到的后端地址。
VITE_DEV_PROXY_TARGET=http://127.0.0.1:8000
