# 多模态教学智能体 · 产品与工程规格说明书（SPEC）

| 项 | 内容 |
|---|---|
| 项目代号 | **析知 XiZhi**（已定稿） |
| 文档版本 | **v2.1** |
| 状态 | **已冻结基线**，后续所有开发以本文档为准 |
| 建立日期 | 2026-09-16（v2.1 更新于 2026-09-17） |
| 赛事 | 深大计软 & 腾讯云 粤港澳大湾区 AI Coding 创新大赛 · 方向一「AI + 教学管理助手」 |
| 赛题 | 多模态教学智能体：支持图文素材智能解析、知识点结构化抽取与交互式答疑辅导的智能教学辅助工具 |
| 交付截止 | **2026-09-26（周六）23:59**（硬截止，截止后不可修改） |
| 团队 | 3 人 · 计算机与软件学院 · 人工智能专业 · **每人每天 8 小时 → 10 天共 240 人时**（见 §8.1）。跨专业组队（+2）**确定不满足，已放弃** |

> **文档权威性约定（重要）**
> 1. 本 SPEC 是项目的唯一权威源。**任何偏离本 spec 的实现都视为缺陷**，除非先修改本 spec。
> 2. 变更流程：发现需要变更 → 在 §12 变更日志登记（谁、何时、为什么）→ 修改对应章节 → 升版本号（v1.0 → v1.1）→ 再改代码。
> 3. 本 spec 中的**验收标准（§7）全部为可测指标**，是"做完了没有"的唯一判据，不是愿景描述。
> 4. 标「待核实」的条目表示信息未确认，**不得据此做实现决策**，必须先闭环。

---

## 目录

1. [项目背景与目标](#1-项目背景与目标)
2. [产品定义](#2-产品定义)
3. [术语表](#3-术语表)
4. [系统架构与技术方案](#4-系统架构与技术方案)
5. [功能规格](#5-功能规格)
6. [关键设计决策与取舍记录](#6-关键设计决策与取舍记录)
7. [验收标准](#7-验收标准)
8. [排期与里程碑](#8-排期与里程碑)
9. [团队分工](#9-团队分工)
10. [风险清单与降级预案](#10-风险清单与降级预案)
11. [待核实项（阻塞项）](#11-待核实项阻塞项)
12. [变更日志](#12-变更日志)
13. [附录](#13-附录)

---

## 1. 项目背景与目标

### 1.1 赛事约束（来自《赛事手册》）

| 约束 | 内容 | 对本项目的影响 |
|---|---|---|
| 开发周期 | 9/16 开通账号 → 9/26 23:59 提交，共 **10 天** | 必须冻结范围，功能只减不加 |
| 作品形态 | **需部署至浏览器环境，提供可直接访问的在线链接**（手册 §二 作品要求，必需材料）；赛事方补充：**提交 demo 即可，不用部署到正式环境**；LearnBuddy 上的智能体可为 web demo 或专家智能体 | **两条一起看 = 门槛降低但要求仍在**：不做生产级环境（域名备案 / 运维 / SLA），但**必须有一个评审期能打开的稳定地址**。定稿走混合形态（S8 / D-16），**轻量部署必须做** |
| 源码 | 提交 GitHub/Gitee 仓库链接 | 代码质量、提交历史、README 都是被看的部分 |
| 附加材料 | 3 分钟 Demo 视频、作品介绍 PPT、**LearnBuddy 历史对话记录** | 三项都要在 D10 前完成，不可压缩到最后一天 |
| 算力 | 2500 Credits/队（初赛） | 用于 LearnBuddy 对话与生成 |
| 加分 | 跨专业组队 +2 / 社媒传播 +1 | ⚠️ **跨专业确定不满足**（三人同院系同专业），**已放弃、不再投入任何时间**；仅剩社媒传播 +1 可争取，必须刻意规划（见 §5.6） |

### 1.2 评分维度 → 设计动作映射

评分即设计的靶子。每一项都必须有对应的、可被评委当场感知的设计动作：

| 评分维度 | 权重 | 本项目的对应动作 |
|---|---|---|
| 技术创新性 | 30% | ① 知识点 **DAG 依赖图 + 学习路径拓扑推荐**；② 答疑的 **苏格拉底式状态机**（探测→提示→讲解的显式降级规则）；③ 每个回答挂 **溯源卡片**（可点回原文页码），把"不许幻觉"做成可见的产品语言而非内部约束 |
| 工程完整性 | 25% | 前后端分离 + 数据库 + ORM；核心 API 单测；Docker 一键起；清晰的目录结构与架构文档 |
| AI 工具使用 | 25% | **开发全程在 LearnBuddy 留痕**（一举两得：既是工作方式，又是提交材料）；Prompt 版本化管理；LLM Provider 抽象层 |
| 用户体验 | 20% | 溯源可点、进度可见、失败可恢复；诊断面板实时显示"卡点"；空/错/慢三种状态都有设计 |

### 1.3 项目目标（一句话）

> 让一份《计算机网络》课上真实拿到的教学材料（讲义 PDF、板书照片、讲课录音），在 3 分钟内变成一个**可查询、可溯源、能引导学生自己学会**的结构化知识体。

### 1.4 成功定义

- **及格线**：三个 Stage 端到端跑通，公网链接可访问，核心验收指标达标。
- **拿奖线**：把 §1.5 的创新点（教学依赖 DAG）做实到可验证 —— 让评委在 3 分钟内看到"这不只是个套壳聊天框"。

### 1.5 创新点与差异化定位（v1.2 新增）

> 完整调研过程、竞品数据与文献出处见 [`docs/innovation.md`](docs/innovation.md)。

**主创新点**

> **AI 预抽取 + 人工校验的知识点依赖图构建，并首次把「教学依赖图必须无环」作为工程不变量强制保证；据此生成可解释的学习路径与卡点根因回溯。**

**为什么这是空白（一图看清）**

| | 图只是检索图（无教学依赖语义） | 图带教学依赖语义 |
|---|---|---|
| **能读任意教师材料** | DeepTutor（3.4 万★，GraphRAG/LightRAG 实体关系图）、Understand Anything、LightRAG / GraphRAG 生态 | 🎯 **本项目**（开源侧空白） |
| **不能读材料**（数据靠人工编） | Excel 知识点清单、传统课标大纲 | Marble Skill Taxonomy（3.8 千★，人工标注）；open-skills（职场技能，人录） |

- **DeepTutor** 把"检索增强问答"做到了工业级，但它 README 全文**没有出现 `DAG` / `prerequisite` / `dependency graph`**，也没有知识点难度分级 —— 它不回答"这个知识点必须先学什么"。
- **Marble Skill Taxonomy** 有 3221 条前置边，但**前置关系是人工标注的**、只覆盖小学英美课标、**不读材料**。
- **学术侧**（Lu et al. AAAI 2019 的 DsCE + iPRL、BEA 2023 的 Auto-req）证明了这件事做得到，但**都停留在论文与数据集，没有可用实现**。

**四个可交付的子能力**

| # | 子能力 | 相对现有工作的位置 |
|---|---|---|
| 1 | **自动抽取**前置依赖边 | 相对 Marble：人工标注 → 自动抽取 |
| 2 | 边带 `hard`/`soft` + `reason` + **原文溯源** | 借鉴 Marble 被验证过的字段设计，但**每条边都能回到材料原文** |
| 3 | 写库前 **DAG 环路校验与剪枝** | **开源侧没有人做**；"教学依赖图必须无环"作为工程不变量强制 |
| 4 | 卡点诊断**沿前置边反向回溯**根因 | 相对 DeepTutor：它追踪会话记忆里的薄弱点，**不知道知识点之间的依赖** |

**诚实边界（必须写进 PPT）**：我们**不是发明了这个问题**——AAAI 2019 就研究过。贡献在于**把已有学术方法用 LLM 重新实现，并补上工程侧缺失的一致性约束与可溯源要求**。这个定位比"我们首创了 XX"更经得起追问。

**核心设计：结构线索 × 语义线索双通道融合**（详见 `docs/innovation.md` §5）

```
结构线索（免费）：章节/小节顺序天然给出方向
语义线索（LLM）：判定 (A,B) 是否成立前置关系，带 hard/soft + reason
      ↓
融合与冲突标记 ←★ 关键：语义与结构冲突时不静默采纳，降 confidence 并标 needs_review
      ↓
写库前 DAG 校验 → 有环则按 confidence 升序剪边 + WARN 日志
```

对应验收指标见 §7.5（B1-1 ~ B1-6），其中 **B1-4「结构-语义冲突检出率 100%」验证的不是模型准不准，而是系统在模型不确定时是否诚实** —— 这比单纯报准确率更有说服力。

> **✅ 表述已定稿（v1.5，D-15 已拍板）**：因赛事不允许调用第三方 API，抽取环节为「AI 预抽取 + 人工校验」。**正式表述为**：
>
> > **AI 预抽取 + 人工校验的知识点依赖图构建，并首次把「教学依赖图必须无环」作为工程不变量强制保证。**
>
> 理由：我们相对 Marble 的真正差异**不是"是否用了 AI"**，而是 **Marble 是静态数据集、不读材料、无法扩展；我们的管线能读任意材料并持续产出** —— 这一点即使中间有人工校验环节也成立。重心明确移到**环路校验 + 学习路径 + 卡点根因回溯**这套确定性工程，它在开源侧仍无同类实现。完整论证见 §4.8.5。
>
> **PPT / 答辩口径**：不说"全自动抽取"，说"**AI 干粗活 + 人做裁决**"，并立刻补一句"教育场景里纯 AI 抽取本来就不可信，人工校验是更专业的产品形态，我们的 `needs_review` 机制就是为它设计的"。

---

## 2. 产品定义

### 2.1 目标用户

| 角色 | 场景 | 核心诉求 |
|---|---|---|
| 学生（主） | 老师发了一份讲义/拍了板书，看不懂、做题卡住 | 别直接给答案，帮我搞懂我卡在哪 |
| 教师/助教（次） | 想把已有材料沉淀成结构化的知识点体系 | 省去手工整理知识点的重复劳动 |

> **初赛只服务"学生自学"这一个主场景**，教师侧仅作为材料录入者出现，不做教学管理功能（见 §2.4 非目标）。

### 2.2 核心用户旅程（Demo 的主线，视频就按这条拍）

1. 进入「素材工作台」，拖入一份《计算机网络》第四章讲义 PDF + 一张「子网划分」题目板书照片 + 一段 2 分钟讲解录音。
2. 系统解析，产出带页码锚点的 Markdown；右侧显示**素材清单**：文件名、类型、解析方式、页数、**存疑处**。
3. 一键「抽取知识点」，得到 **章 → 节 → 知识点** 三级结构，每个知识点带难度、前置、典型例题、常见误区、原文溯源。
4. 切到「知识图谱」，看到依赖关系的 DAG；点任一知识点，查看详情与学习路径建议。
5. 切到「答疑辅导」，提问"为什么 TCP 建立连接要三次握手，两次不行吗"。系统**不给答案**，先反问；学生答错两次后降级为直接讲解。
6. 每轮结束右侧面板给出三件事：涉及的知识点、卡在哪一步、下一步建议练习。
7. 全程可点溯源卡片回到讲义原文页码。

### 2.3 范围边界（IN SCOPE）

| 编号 | 范围项 | 说明 |
|---|---|---|
| S1 | 素材录入 | PDF（文本版/扫描版）、Word（.docx）、PPT（.pptx）、图片（JPG/PNG）。**音频不做**（D-08） |
| S2 | 素材解析 | 统一转 Markdown（带页码/行号锚点）→ OCR / 多模态理解 → 按字段抽取（题型、题干、选项、答案、所属知识点） |
| S3 | 知识点结构化 | 三级结构「章—节—知识点」；五要素：所属章节、难度 1–5、前置知识点、典型例题、常见误区；全字段溯源 |
| S4 | 学习路径 | 基于前置依赖 DAG 的拓扑排序推荐 |
| S5 | 交互式答疑 | 检索增强 + 苏格拉底式引导 + 兜底降级 + 越界拒答 |
| S6 | 答疑产出 | 每轮输出：涉及知识点 / 卡点定位 / 下一步练习 |
| S7 | 交付物 | 公网链接（**口径待确认，见 V-20**）、源码仓库、Demo 视频、介绍 PPT、LearnBuddy 对话记录 |
| S8 | **作品形态（v1.9 定稿）** | **混合形态**：LearnBuddy **专家智能体**（零门槛交互入口，其页面本身也是一个"可直接访问的链接"）+ **自建 Web Demo**（承载知识图谱、依赖 DAG、学习路径、卡点回溯等技术深度）。**Web Demo 必须做轻量部署并提供稳定链接**（见 §4.7）。详见 [`docs/delivery-form.md`](docs/delivery-form.md) |

**⚠️ 部署口径（v1.9 修正，两条要求一起看）**

| 要求来源 | 原文 | 解读 |
|---|---|---|
| 手册 §二 | 「作品须**部署至浏览器环境**，提供**可直接访问的在线链接**」（必需材料） | **必须有稳定可访问的地址** —— 评审按手册走 |
| 赛事方口头 | 「提交 demo 即可，**不用部署到正式环境**」 | **门槛降低**：不需要域名备案、生产级运维、SLA、负载均衡 |

**兼容落点 = 一个"跑起来就行"的轻量部署。** 不买高配、不做正式环境，但**必须有一台能常驻的机器把服务跑着**，让评委在评审期随时点得开。

> ❌ **已排除的偷懒方案**：cloudflared 临时隧道只能在演示时活着，**评委评审期点开会 404，等于没有链接**。它只能作为演示当天的备用通道，不能作为交付链接。

**作品形态的硬边界（无论最终选哪种形态都不变）**

1. **核心创新点必须有代码承载** —— 依赖 DAG 的环路校验与剪枝、学习路径拓扑排序、卡点根因回溯**都是纯算法**，平台配置做不出来。没有代码，技术创新性 30% 与工程完整性 25% 同时落空。
2. **源码仓库与 Demo 视频一个都不能省** —— 它们是"技术深度"与"工程完整性"的唯二证据（即使"不用正式环境"）。
3. **苏格拉底式答疑的「代码层强制降级」不妥协**（§5.3 R3）—— 主实现放在 Web Demo；专家智能体里只放轻量入口版，且 PPT 要讲清哪边是主实现。

### 2.4 非目标（OUT OF SCOPE，初赛明确不做）

以下条目**明确不做**，任何想要加进来的提议都必须先走 §12 变更流程，且默认拒绝：

- ❌ 用户注册、登录、权限、多租户
- ❌ 移动端适配（**只做桌面端**，基准视口 1440×900）
- ❌ 材料库共享、协作编辑、评论
- ❌ 长期学习追踪、错题本、学习报告历史
- ❌ 自研 OCR / ASR 模型（一律调用现成能力）
- ❌ 在线组卷、自动批改试卷
- ❌ 多语言（仅简体中文）

### 2.5 产品原则（用于裁决争议）

1. **宁缺毋错**：检索不到就明说"材料里没有"。宁可显得笨，不可编造。
2. **显式不确定性**：解析存疑处主动标出来，不做"看起来很完美"的假象。诚实是可被感知的加分项。
3. **先结论后细节**：所有输出先给结论，再给依据。
4. **可见的溯源**：任何来自材料的断言，都能一点回到原文。
5. **上游优先**：Stage 1/2 的准确率优先级高于 Stage 3 的交互花活。

### 2.6 目标学科与材料范围（V-01 已闭环）

**学科：计算机网络**（大学本科计算机类专业核心课）
**教材：*Computer Networks: A Systems Approach*（Larry Peterson & Bruce Davie, 6th Edition，许可 CC BY 4.0）** —— 见 `docs/materials-and-licenses.md`

**为什么锁定这门课**（这不是随便挑的，是为"能验证"服务的）：

| 理由 | 说明 |
|---|---|
| 知识点有真实依赖结构 | 拥塞控制 → 滑动窗口 → 可靠传输 → TCP 报文段格式，依赖链是课程本身固有的，不是我们硬造的。**DAG 与学习路径推荐在这里才有说服力** |
| 误区丰富且经典 | "三次握手为什么不是两次""CSMA/CD 和 CSMA/CA 差在哪""慢启动到底慢在哪""子网划分算错掩码"—— `kp_misconceptions` 有真实素材可写，不是 LLM 泛泛而谈 |
| 典型例题天然存在 | 子网划分、CRC 计算、奈氏准则/香农定理、时延与吞吐量计算，都是可判对错的硬题目 |
| 术语密集且含大量英文缩写 | 对 OCR / 视觉识别是压力测试，也为"术语归一化"提供了真实场景 |
| 材料易得且合法 | 教材是 **CC BY 4.0 开放许可**，章节原文与 PDF/EPUB 都可自由使用（署名即可）；扫描件与板书可自造 |
| 评委是计算机专业 | 双刃剑：抽错了会被一眼看穿；反过来，抽对了就是最有力的证明 |

**知识骨架（评测基准，V-19 已闭环：教材定为 Peterson & Davie 6th）**

教材的章节体系直接作为评测集（E-1）的金标准骨架。**做深 3 章、节选覆盖，不追求全书**：

| 章 | 本章子节 | 覆盖策略 | 主要知识点聚类（用于构建 E-1） |
|---|---|---|---|
| 第 1 章 Foundation | 1.1–1.5 | 轻量（1 节） | 分层体系结构、协议与服务、性能指标（带宽、时延、时延带宽积、吞吐量） |
| 第 2 章 Direct Links | 2.2–2.8 | 轻量（1–2 节） | 编码、成帧、差错检测、**可靠传输**、多路访问（CSMA/CD 系）、无线与 CSMA/CA |
| **第 3 章 Internetworking（网络层）** | 3.1 / 3.3 / 3.4 | **做深** | 交换基础、**IP 数据报与编址**、子网与转发、**路由（距离向量 / 链路状态）**、自治系统与域间路由 |
| 第 4 章 Advanced Internetworking | 4.1 / 4.2 | 轻量 | 全局互联网结构、IPv6 |
| **第 5 章 End-to-End Protocols（传输层）** | 5.1 / **5.2** | **做深** | UDP 与解复用、**TCP 可靠字节流**、滑动窗口与流量控制、超时重传与 RTT 估计、连接建立与拆除 |
| **第 6 章 Congestion Control（拥塞控制）** | 6.1 / 6.2 / **6.3** | **做深**（★已验证） | 资源分配问题、排队规则、**AIMD / 慢启动 / 快速重传与快速恢复**、拥塞控制演进 |
| 第 7 章 End-to-End Data | 7.1 | 轻量 | 表示格式与数据编码 |
| 第 8 章 Network Security | 8.2 / 8.4 | 轻量 | 密码学构件、认证协议 |
| 第 9 章 Applications | 9.1 / 9.3 | 轻量 | 传统应用（HTTP / DNS / SMTP）、基础设施应用 |

**为什么第 6 章也做深**：冒烟测试已经用它的真实原文跑通了 P3/P4/P10 全链路（9 个知识点、10 条边、环数 0，见 `docs/extraction-channel.md` §5），且**它的依赖链是全课程最密的**——拥塞窗口 → 加性增/乘性减 → AIMD 不对称性 → 慢启动 → 快速恢复，天然就是 DAG 的最佳展示案例。

> **章节名称与编号已按教材在线版核对过**，可直接使用。若后续改用其他版本（如 7th Edition 预览版），须重新核对并走变更流程。

**⚠️ 关于材料语言（必须准备答辩说法）**

教材是英文的，产品界面是中文的。这在答辩时**一定会被问**。标准回答：

> 「材料语言与界面语言是两件事。系统做的是'从任意材料抽取知识结构'，中文材料同样支持 —— 我们的 OCR 是全本地中文识别的，抽取 Prompt 也是中文的。选这本教材是因为它是 **CC BY 4.0 开放许可**，可以合法地在开源仓库与 Demo 视频里使用；而且它覆盖的网络层/传输层/拥塞控制三章，恰好是本课程依赖关系最密的部分。」

**建议（低成本高收益）**：再补 **1 份中文材料（M-H）**——老师授权的课件，或自己写的 2 页中文讲义。**用一个中文材料走通全流程，就能证明系统不是为某本英文教材定制的**。约 2 人时。

**材料清单（评测集与演示材料，D2 前必须凑齐）**

> **⚠️ 材料来源必须以开放许可或已获授权为前提**，不得使用未授权的商业教材扫描件。可选来源、许可核实与合规动作清单见 **[`docs/materials-and-licenses.md`](docs/materials-and-licenses.md)**。

| 编号 | 材料 | 类型 | 用途 | 建议来源（许可已核实） |
|---|---|---|---|---|
| M-A | **Ch 3 Internetworking** 节选（3.1 / 3.3 / 3.4） | pdf_text | A1-2 原文抽取准确率；网络层子图 | Peterson & Davie 6th（**CC BY 4.0**） |
| M-B | **Ch 5 End-to-End Protocols**（5.1 / 5.2）+ **Ch 6 Congestion Control**（6.1 / 6.2 / 6.3） | pdf_text | 传输层 + 拥塞控制子图；**DAG 演示主体（Ch 6 已验证）** | 同上 |
| M-C | 一页扫描版讲义 | pdf_scan / image | A1-3 关键字段抽取；OCR 压力测试 | **把教材某页打印后自行扫描** —— 零版权风险 |
| M-D | 「IP 编址与子网」课堂板书照片 2 张 | image | OCR 手写体压力测试 | **自行手写板书后拍照** —— 零风险，且比找现成图片更贴真实场景 |
| M-E | ~~课堂讲解录音~~ | ~~audio~~ | ~~音频链路~~ | **已砍（D-08）** |
| M-F | 老师课件 PPT 一份 | pptx | 类型覆盖 | 授课老师自己的课件（**需取得使用同意**） |
| M-G | RFC 节选 | pdf_text | 一手权威材料，增强溯源举证 | RFC 5681（TCP Congestion Control）、RFC 793（TCP）—— IETF 公开文档 |
| **M-H** | **中文材料 1 份**（建议） | pdf_text | **证明系统不依赖某本英文教材**，准备答辩说法 | 老师授权课件，或**自制 2 页中文讲义**（约 2 人时） |

> **中文材料的现实问题**：计算机网络领域**几乎不存在中文开放教材**。三条路径（教师授权 / 中英混用 / 自制）及推荐度见 `materials-and-licenses.md` §3。**建议优先争取教师授权，同时用英文开放教材兜底，不要拖到 D5 还在纠结材料。**

> **⚠️ 术语基准规则（冒烟测试发现，务必遵守）**：英文教材与中文教材的术语体系**不是一一对应**的（例：Peterson & Davie **没有独立的 "Congestion Avoidance"** 概念，对应语义由 "additive increase" 承担）。
> **规则：E-1 金标准必须以"实际使用的那份材料"为基准建立；若同时使用中英文材料，须分别建基准，不得混用** —— 否则会凭空产生"漏抽"的假阴性。

**评测集（三份，由非前端成员制作，D4 前完成）**

| 编号 | 内容 | 规模 | 对应验收 |
|---|---|---|---|
| E-1 | 金标准知识点集：人工标注 2 个章节的章-节-知识点 + 难度 + 前置依赖 | ≥ 60 个知识点 | A2-1、A2-5 |
| E-2 | 结构化字段核对样本（题干/选项/答案/题型） | 20 题 | A1-3 |
| E-3 | 答疑问题集：基于材料的问题 + 材料外问题 | 30 + 20 题 | A3-1、A3-2 |

---

## 3. 术语表

| 术语 | 定义 |
|---|---|
| **素材（Material）** | 用户上传的单个原始文件 |
| **块（Block）** | 素材解析后的最小 Markdown 单元，携带 `page_no` / `line_start` / `line_end` 锚点，是溯源的原子单位 |
| **知识点（KP, Knowledge Point）** | 三级结构的叶子节点，必须挂靠一个"节"，带五要素 |
| **五要素** | 所属章节、难度（1–5）、前置知识点、典型例题、常见误区 |
| **溯源（Grounding）** | 从知识点/回答回指到具体 `material_id` + `page_no` + 原文片段的能力 |
| **越界（Out of Scope Query）** | 学生提问所涉内容在已入库材料中不存在 |
| **苏格拉底式引导** | 提问 → 提示 → 讲解 的三段式引导状态机，见 §5.3 |
| **素材清单** | 每批素材处理完输出的核对表：文件名、类型、解析方式、存疑处 |
| **DAG** | 知识点前置依赖构成的有向无环图，禁止出现环 |

---

## 4. 系统架构与技术方案

### 4.1 总体架构

```
┌──────────────────────────── 浏览器（前端静态托管） ────────────────────────────┐
│  React 18 + Vite + TS + TailwindCSS                                          │
│  ├─ 素材工作台   ├─ 知识图谱   ├─ 学习路径   ├─ 答疑辅导（SSE 流式）            │
└───────────────────────────────┬──────────────────────────────────────────────┘
                                │ HTTPS / REST + SSE
┌───────────────────────────────▼──────────────────────────────────────────────┐
│  后端 API 服务（FastAPI + Uvicorn，单容器）                                    │
│  ┌────────────┬─────────────┬──────────────┬──────────────────────────────┐  │
│  │ 素材解析层  │ 结构化抽取层 │  检索层       │  答疑编排层（苏格拉底状态机）   │  │
│  │ parse/     │ extract/    │ retrieve/    │  tutor/                      │  │
│  └─────┬──────┴──────┬──────┴──────┬───────┴───────────┬──────────────────┘  │
│        │             │             │                   │                     │
│        └─────────────┴──────┬──────┴───────────────────┘                     │
│                             ▼                                                │
│                  LLM Provider 抽象层（统一接口）                               │
│                  构建期抽取辅助：LearnBuddy（主）/ 本地 3B（兜底）  ｜ PaddleOCR  │
│                             │                                                │
│                             ▼                                                │
│                     SQLite + SQLAlchemy ORM（可切 PostgreSQL）                 │
│                     materials / blocks / chapters / sections / kp / ...      │
└──────────────────────────────────────────────────────────────────────────────┘
```

**架构形态**：前后端分离（独立代码目录与构建产物）+ 数据库持久化。
**部署形态**：**单容器**——FastAPI 同时挂载前端构建产物（`/` 走静态文件，`/api` 走后端）。原因：只有一个公网 URL，无跨域配置，部署成本最低，但不牺牲前后端分离的代码结构与工程完整性得分。

### 4.2 技术栈（冻结）

| 层 | 选型 | 理由 | 冻结 |
|---|---|---|---|
| 前端框架 | React 18 + TypeScript + Vite | 生态成熟、构建快、类型安全 | ✅ 不可改 |
| 样式 | TailwindCSS | 10 天内出效果最快 | ✅ |
| UI 组件 | shadcn/ui（按需拷贝，非依赖） | 可控、无版本绑架 | ✅ |
| 图可视化 | Cytoscape.js（DAG）/ AntV G6 | 知识图谱与学习路径 | ✅ |
| 公式渲染 | KaTeX | 教学材料必含公式 | ✅ |
| PDF 预览 | PDF.js | 溯源跳转需要预览到页 | ✅ |
| 后端框架 | **FastAPI** + Uvicorn | **决定性理由：Stage 1 的解析生态在 Python**（PyMuPDF / python-docx / python-pptx / PaddleOCR），换 Node 要多花 2–3 天 | ✅ 不可改 |
| ORM / 迁移 | SQLAlchemy 2.x + Alembic | 体现工程规范 | ✅ |
| 数据库 | **SQLite（WAL 模式）** | 免运维、免云资源、单文件备份；单机 Demo 场景性能充足 | ✅ |
| 向量检索 | **SQLite 存 embedding BLOB + NumPy 暴力余弦检索** | 知识点规模在千级，暴力检索 <10ms，**不上向量数据库** | ✅ |
| 关键词检索 | SQLite FTS5（BM25） | 与向量检索做混合召回 | ✅ |
| 异步任务 | FastAPI `BackgroundTasks` + 任务表轮询 | 解析耗时长，不上 Celery/Redis | ✅ |
| 抽取辅助 · 主路径 | **LearnBuddy 平台**（赛事唯一指定，2500 Credits） | 赛事规则不允许产品调用第三方 API；开发期用 LearnBuddy 抽取恰好对应「AI 工具使用 25%」评分项（§4.8.1） | ✅ |
| 抽取辅助 · 兜底 | **本地 3B 量化模型**（Ollama + `qwen2.5:3b` Q4） | 开发机 8GB 内存，7B 跑不动；3B 约 2GB 可跑，构建期一次性、可慢（§4.8.1） | ✅ |
| OCR | **PaddleOCR（纯本地）** | 无第三方 API 约束下唯一的图片解析路径；仅 3 张图片，人工校对兜底（§4.8.2） | ✅ |
| ~~产品运行时 LLM~~ | **不引入** | §4.8.3：运行期检索/状态机/图算法全为纯算法，回复用模板 + 原文片段拼装 | ✅ 冻结 |
| 容器 | Docker + docker-compose | 一键起 | ✅ |

> **技术栈冻结规则**：以上选型在 D1 之后不得变更。若确需变更，必须走 §12 变更流程并说明对排期的影响。

### 4.3 LLM Provider 抽象层（强制）

**所有**大模型调用必须经过统一抽象层，禁止在业务代码里直接 `requests.post` 到某家 API。

**Provider 路由表**

> ⚠️ **v1.4 状态：保留但不启用。** 下表是**构建期抽取阶段**的理想配置；在"赛事不允许调用第三方 API"的约束下（§4.8），实际执行载体为 **LearnBuddy（主）+ 本地 3B（兜底）**。契约本身不变 —— P1~P10 定义的是**输入输出结构**，与执行载体无关。

关键约束：**纯文本模型不能读图**，所以 Stage 1 的图片/扫描页链路必须由**独立的视觉能力**承担 —— v1.4 起由 **PaddleOCR（本地）** 承担（§4.8.2）。

| 任务 | Provider | 理由 |
|---|---|---|
| P1 图片 / 扫描页 → Markdown | 阿里云百炼 **Qwen-VL** | 需要视觉能力 |
| P2 题目字段抽取（走图片路径时） | 阿里云百炼 **Qwen-VL** | 同上 |
| P2 题目字段抽取（走 Markdown 路径时） | DeepSeek | 纯文本，便宜 |
| P3 章节目录识别 | DeepSeek | 纯文本短输入 |
| P4 逐节知识点抽取 | DeepSeek | 量最大的调用，务必用经济档 |
| P5 查询改写 | DeepSeek | 短输入短输出 |
| P6 检索重排与越界判定 | DeepSeek | **必须与 P7 同源**，否则"判定相关性"与"生成回复"两套判断会割裂 |
| P7 苏格拉底式教练 | DeepSeek | 对中文语气与指令遵循要求最高 |
| P8 卡点诊断 | DeepSeek | 纯文本 |
| **P10 前置依赖边抽取**（v1.2 新增） | DeepSeek | 本项目的**主创新点**所在，见 §1.5 与 `docs/innovation.md` §5 |

**v1.4 定位变更（重要）**：上表列的是**构建期抽取阶段**的理想 Provider 配置。在"赛事不允许调用第三方 API"的约束下（§4.8），实际执行载体变为 **LearnBuddy 平台（主）+ 本地 3B 模型（兜底）**，Provider 路由表**保留但不启用**。

- **保留的理由**：① 若 §4.8.7 向主办方确认后规则其实允许，一行配置即可切回，不用改任何业务代码；② 它是"架构分层清晰"的工程证据，本身对应「工程完整性 25%」。
- **P1~P10 契约全部继续有效**：契约定义的是**输入输出结构**，与执行载体无关 —— 无论用 LearnBuddy、本地 3B 还是云端 API，抽取产出的 JSON 结构必须一致。这正是当初坚持做 Provider 抽象的回报。

**工程要求**：路由由配置文件 `config/llm_routing.yaml` 驱动，**不硬编码在业务逻辑里**。任一 Provider 不可用时，回落到另一家的同档模型，并把 `degraded=true` 写进 `llm_calls` 表。

```python
class LLMProvider(Protocol):
    async def complete(
        self,
        messages: list[Message],
        *,
        response_schema: dict | None = None,   # 传入 JSON Schema 则强制结构化输出
        temperature: float = 0.0,              # 默认 0，保证可复现
        max_tokens: int = 4096,
    ) -> LLMResult: ...

    async def complete_vision(
        self,
        images: list[bytes],
        prompt: str,
        *,
        response_schema: dict | None = None,
    ) -> LLMResult: ...

@dataclass
class LLMResult:
    text: str
    parsed: dict | None          # 结构化输出解析结果（schema 校验后）
    usage: TokenUsage
    provider: str
    model: str
    latency_ms: int
    raw: dict                    # 原始响应，便于排查
```

**硬性要求**：
- 抽取类调用一律 `temperature=0.0` 并固定 `seed`（若provider支持），保证**同输入可复现** —— 这是 §7 里"可重跑 diff"验收项的前提。
- 结构化输出失败时的重试策略：**最多重试 2 次**，第 2 次在 prompt 中追加错误信息；仍失败则落库 `status='failed'` 并把原始文本返回给用户，**绝不静默丢弃**。
- 每次调用必须落 `llm_calls` 日志表（prompt 版本、token、耗时、是否校验通过），用于质量归因。

### 4.4 解析策略（Stage 1 核心取舍）

**总原则：统一转 Markdown 再抽结构，不直接对着原始文件做问答。**

| 素材类型 | 主路径 | 兜底路径 |
|---|---|---|
| PDF（文本层可用） | PyMuPDF 抽文本 + 版式 → Markdown，保留页码锚点 | — |
| PDF（扫描版 / 无文本层） | **PaddleOCR（纯本地）** → 版式重建规则 → Markdown | 人工校对（图片类素材仅 3 张，成本极低） |
| Word (.docx) | python-docx / markitdown → Markdown | — |
| PPT (.pptx) | python-pptx 抽文本框与备注 → Markdown | 转图片后走 OCR |
| 图片（板书/题目照片） | **PaddleOCR（纯本地）** → 规则/模板抽字段 → 人工校验 | 人工转写（板书手写体识别率低时人工反而更快） |
| 表格 | 一律转 Markdown 表格；复杂表格转 HTML 表格嵌在 Markdown 中 | — |

**决策理由（v1.4 修订）**：原方案（多模态大模型直读）在"无第三方 API"约束下不可用。改以 **PaddleOCR 纯本地解析 + 人工校验** 承担，代价是版式与手写体还原能力下降 —— 但**素材侧只有 3 张图片，人工兜底的边际成本极低**，足以补平短板。这一取舍如实写进产品与 PPT，不夸大。

**「存疑处」的定义（必须实现）**：以下情况必须在素材清单中显式标出，不得掩盖：
- OCR/视觉识别confidence 低于阈值
- 题目字段缺失（如有题干无选项、有选项无答案）
- 章节结构与标题层级无法判断

### 4.5 检索方案（Stage 3 的地基）

**混合召回**：
1. 关键词召回：FTS5 BM25，取 top 20。
2. 向量召回：query embedding 与 `kp_embeddings` 余弦相似度，取 top 20。
3. 融合：RRF（Reciprocal Rank Fusion）合并排序，取 top 8。
4. **重排**：LLM 对 top 8 做相关性判定，输出 `relevant_kp_ids` + `is_out_of_scope`（这一步同时承担越界判定）。

**越界判定双保险**（对应"检索不到就明说"这条硬规矩）：
- 保险一：融合分 < 阈值 → 直接判越界。
- 保险二：LLM 重排阶段输出 `is_out_of_scope=true`。
- 两者任一命中即拒答，回复模板：
  > 材料里没有这个内容。我能查到的是：〔列出最接近的 2–3 个知识点〕。你可以先问我这些，或者把相关材料传上来。

### 4.6 数据模型

完整字段定义见 **[`docs/data-model.md`](docs/data-model.md)**。核心表：

```
materials ──1:N──> material_blocks        (溯源原子单位，带页码/行号)
materials ──1:N──> chapters ──1:N──> sections ──1:N──> knowledge_points
knowledge_points ──N:M──> knowledge_points   (kp_prerequisites，前置 DAG)
knowledge_points ──1:N──> kp_examples / kp_misconceptions
knowledge_points ──1:1──> kp_embeddings
qa_sessions ──1:N──> qa_turns             (含检索依据、接地标记、诊断结果)
llm_calls                                 (可观测性，质量归因)
jobs                                      (异步任务状态)
```

**强制约束**：
- `knowledge_points` 的 `source_material_id` / `source_page` / `source_quote` **非空**（未通过则 `needs_review=1`）。
- 前置依赖必须无环，服务启动时与每次写入后做 DAG 校验，检测到环则拒写并记日志。

### 4.7 部署与演示形态（v1.9：轻量部署必须做）

> ⚠️ **口径澄清（重要）**
>
> 手册 §二 明确要求「作品须部署至浏览器环境，提供**可直接访问的在线链接**」，且这是**必需提交材料**；赛事方补充「不用部署到正式环境」。
>
> **两条一起看 = 门槛降低，但要求仍在。** 正确落点是：**做一次轻量部署，提供稳定链接；但不做生产级环境**（无域名备案、无运维、无 SLA、无负载均衡）。
>
> ❌ **不可接受的偷懒方案**：只用 cloudflared 临时隧道或本地运行 —— **评委在评审期点开会 404，等于没有链接**。隧道只能作演示当天的备用通道。

下表为轻量部署的具体方案。成本与工时变化见 [`docs/delivery-form.md`](docs/delivery-form.md) 与 §6.2。

| 项 | 方案 |
|---|---|
| 构建 | 前端 `vite build` → `dist/`；后端 Dockerfile 多阶段构建（前端产物 COPY 进后端镜像） |
| 运行 | 单容器，暴露 8000 端口；SQLite 与上传文件挂载到 volume |
| 主机 | **买最轻量的**（v1.9 修正）：轻量应用服务器 2 核 2G / 50G 盘。**务必先做高校学生实名认证领代金券再下单**（可领 300 元无门槛券，很可能把成本压到 ¥0）。**不要为"保险"加配** |
| 公网入口 | 主机公网 IP 直接访问即可 —— **不做域名备案、不强制 HTTPS、不做生产级运维与监控**，这正是"不用部署到正式环境"的含义。想链接好看可花 1 元买新客 cn 域名 |
| 环境变量 | `.env`：`DEEPSEEK_API_KEY`、`BAILIAN_API_KEY`、`LLM_ROUTING_PATH`、`DB_PATH`、`UPLOAD_DIR`、`MAX_UPLOAD_MB`。仓库只提交 `.env.example` |
| **首次上线（v1.9 恢复为硬要求）** | **D5（9/20）必须有稳定可访问的链接** —— 手册明确这是必需材料。上门槛比原计划低（无需正式环境），但**不能不做**。D5 之后每次改动都重新部署 |
| 降级预案 | ① 主机不可用 → 前端静态托管（EdgeOne Pages / CloudStudio）+ 后端独立部署，需补 CORS；② 时间不够 → 允许在 D5 用 Nginx 直接指静态页 + 后端 API，先保证"有链接"这件事成立 |

### 4.8 无第三方 API 约束下的执行方案（D-13 已触发 · v1.4）

> **背景（已确认）**：① **赛事规则不允许作品调用第三方大模型 API**；② 团队开发机为 8GB 内存档位，**跑不动 7B 模型**。

**结论：项目仍然完整可行，且主创新点不受影响。**

理由：主创新点（依赖 DAG 的环路校验、学习路径生成、卡点根因回溯）与苏格拉底状态机**本来就是确定性算法，不需要 LLM 在线运行**。真正受影响的只有两处 —— **构建期的抽取** 与 **视觉链路**。核心判断不变：**要改的不是"换哪个模型"，而是"把 LLM 从关键路径上摘下来"。**

关键事实是 —— **我们的主创新点根本不需要 LLM 在线运行**：

| 环节 | 发生时机 | 是否必须调 LLM |
|---|---|---|
| 解析素材 → Markdown | 构建期，一次性 | 需要（但可离线、可慢） |
| 抽取知识点 | 构建期，一次性 | 需要（同上） |
| 抽取前置依赖边 ★ | 构建期，一次性 | 需要（同上） |
| **检索（FTS5 + 向量）** | 运行期，每次请求 | **不需要**（纯 CPU 算法） |
| **依赖 DAG 环路校验与剪枝 ★** | 构建期 + 运行期 | **不需要**（拓扑排序） |
| **学习路径生成 ★** | 运行期 | **不需要**（拓扑排序） |
| **卡点根因回溯 ★** | 运行期 | **不需要**（反向可达） |
| **苏格拉底状态机** | 运行期 | **不需要**（代码层状态机） |
| 生成自然语言回复 | 运行期 | 需要 → **改为模板 + 检索片段拼装** |

**架构改造：离线构建 + 在线确定性推理**

```
【构建期】开发时一次性跑完，可离线，慢没关系
   解析素材 → 抽知识点 → 抽依赖边 → 全部落 SQLite
   （本地开源模型 / PaddleOCR，或人工辅助校验）

【运行期】评委访问时，每次请求，必须秒级
   检索（FTS5 + NumPy 向量）→ 状态机（代码层强制）→ 图算法（拓扑 / 反向可达）
   → 回复由「模板库 + 检索到的原文片段」组装，可选本地小模型增强
```

**回复模板化的具体做法（降级方案里技术含量最高的一段）**

苏格拉底式回复天然高度模板化，每个 `turn_type` 绑定确定的数据槽位：

| turn_type | 数据来源 | 模板骨架 |
|---|---|---|
| `probe` | `kp_misconceptions.trigger_pattern` + 知识点 | "先想一个问题：如果〔槽位〕，会怎样？" |
| `hint1` | 该知识点的 `summary_md` | "提示一个方向：〔摘要〕。你觉得这跟你的问题有什么关系？" |
| `hint2` | `source_quote`（原文片段） | "再缩小一点 —— 材料里说：『〔原文〕』。现在能对应上吗？" |
| `explain` | `summary_md` + 硬前置的 `summary_md` + `source_quote` | 分步拼接 + 过渡语 + 结尾自查问题 |
| `refuse` | 检索到的最接近知识点列表 | 固定模板（§4.5） |
| `confirm` | `kp_examples` 的变式 | "对，就是这样。〔变式练习〕试试？" |

> **关键论点（可直接写进 PPT）**：这样改之后，**回复质量由"知识点的结构化程度"决定，而不是由模型能力决定** —— 而这恰好是我们的强项。因此 §5.2 的抽取质量变得比以往更关键。

**必须付出的代价（诚实说明）**

| 代价 | 缓解 |
|---|---|
| 无法处理评委现场上传的新材料 | A4-7 预置示例材料；UI 如实说明"演示版预置了计算机网络第 4/5 章材料" |
| 回复自然度下降，模板痕迹可见 | 每个模板做 8–12 个变体轮换；关键处插入原文片段提升可信感 |
| 本地小模型的 JSON 结构化输出遵循率低 | 校验 + 重试 + 宽松解析兜底（P10 契约已有设计） |
| 构建期耗时长（本地 CPU 推理慢） | 一次性，可过夜跑；结果落库后不重复跑（成本控制 C2 已覆盖） |

**三个候选路线与推荐**

| 路线 | 做法 | 优点 | 缺点 | 结论 |
|---|---|---|---|---|
| **A · 离线构建 + 模板化运行期** | 构建期本地跑开源模型；运行期纯算法 + 模板 | 零 API 成本、无外部依赖、演示绝不翻车、可复现 | 回复自然度下降；处理不了新素材 | ✅ **推荐** |
| B · 运行期也用本地模型 | 本地跑模型 + 内网穿透暴露公网 | 保留完整 LLM 答疑体验 | **笔记本必须 24h 在线，评委访问时掉线 = 演示直接失败** | ⚠️ 仅作 A 的可选增强，不可作唯一方案 |
| C · 全人工标注 + 纯算法 | 知识点与依赖边全部手工标注 | 最稳、零技术风险 | **击穿"自动抽取"这个创新点**，退化成 Marble 那种人工标注 | ❌ **底线降级，不到万不得已不用** |

**额外收益（这点必须讲清楚）**：路线 A 不是"退而求其次"，它在多个维度上**比原方案更强**——

- API 成本从 ¥10~30 降到 **¥0**（只剩服务器）
- 响应延迟从首 token 2~3s 降到 **<200ms**（A3-7 直接超额达标）
- A2-7「同输入可复现」变成天然成立
- 演示不依赖任何外部服务，**不会被 API 限流、欠费、抽风毁掉现场**
- 答辩可说：「**我们把关键路径放在确定性算法上，LLM 只做增强** —— 教学场景里'可解释、可复现'比'话术流畅'重要得多。」这是**有说服力的工程论点**，不是辩解。

#### 4.8.1 抽取：三层策略

| 层 | 手段 | 成本 | 定位 |
|---|---|---|---|
| **第一层** | **LearnBuddy 平台**（赛事唯一指定平台，2500 Credits） | 免费（赛事提供） | **主路径**。抽取 Prompt 与 P1~P4/P10 契约一致；结果导出后由脚本入库 |
| 第二层 | **本地 3B 量化模型**（Ollama + `qwen2.5:3b-instruct` Q4，约 2GB） | 免费 | 补充与增量。8GB 内存可跑，CPU 约 5–10 tokens/s，构建期一次性、可过夜 |
| 第三层 | **人工校验工作台** | 约 10 人时 | 质量兜底，**同时是产品功能**（`needs_review` 工作流）；不是权宜之计 |

**为什么第一层是最优解**：赛事要求"以 LearnBuddy 平台为赛事唯一指定平台"，**开发期使用它恰好对应「AI 工具使用 25%」这个评分项**。且"AI 辅助开发"与"产品运行时调用外部 API"是两件不同的事 —— 前者正是赛事给 2500 Credits 的用途。

**规模可行性核算**：材料仅 5–6 份（M-A ~ M-D、M-F），覆盖 2 个章节，约 20–30 个"节"、60–100 个知识点、100–200 条边。按每节一次抽取调用算约 30 次 —— **2500 Credits 绰绰有余**。

#### 4.8.2 视觉链路：改本地 OCR

| 原方案 | 新方案（v1.4） |
|---|---|
| Qwen-VL 多模态直读（图片 → Markdown），一次调用完成 OCR + 字段抽取 | **PaddleOCR（纯本地、免费、CPU 可跑）** → 版式重建规则 → Markdown → 规则/模板抽字段 → 人工校验 |

**影响与缓解**

| 影响 | 缓解 |
|---|---|
| PaddleOCR 对**手写板书**的识别率明显低于多模态模型 | 图片素材只有 3 张（M-C 扫描件 + M-D 板书 2 张），**人工校对一遍仅 1–2 小时**，成本完全可接受 |
| 复杂版式（多栏、公式、表格）还原能力弱 | `uncertain_notes` 显式标注（该机制本来就有，现在更重要） |
| — | **诚实标注**：产品内与 PPT 均写明"图片解析采用本地 OCR + 人工校验"，不夸大 |

> **素材量小在这里反而是优势**：只有 3 张图片，人工兜底的边际成本极低，足以把 OCR 的短板补平。

#### 4.8.3 运行期：完全零外部依赖（沿原设计）

检索（FTS5 + NumPy 向量）→ 状态机（代码层强制降级）→ 图算法（拓扑排序 / 反向可达）→ 回复由「模板库 + 检索到的原文片段」组装。
**成本 ¥0、延迟 <200ms、演示不依赖任何外部服务。**

> **✅ 已落地验证（v2.0）**：其中的**图算法层已经真正实现并挂成了平台 Skill**（`xizhi-graph-infer`），四个算法 + 两组对抗性测试全部实测通过。也就是说「运行期零模型依赖」不再只是设计意图 —— **图推理这一层已经跑在平台上了**。完整验证记录见 [`docs/platform-capability-check.md`](docs/platform-capability-check.md)。
>
> **平台挂载方式（已验证）**：Skill 形态 —— 在 `~/.learnbuddy/skills/` 下建目录（`SKILL.md` + `scripts/`），**平台立即识别，无需重启**。这是混合形态里"专家智能体"能挂载我们自研能力的具体路径。

#### 4.8.4 工时重新分配（总额仍为 185 人时）

| 模块 | v1.2 | **v1.4** | 变化原因 |
|---|---|---|---|
| Stage 1 素材解析 | 40 | **44** | +PaddleOCR 集成与版式后处理（+8）、+图片人工校对（+3）、−Qwen-VL 集成（−4）、−音频（−3） |
| Stage 2 知识点抽取 | 38 | **30** | 抽取改为 LearnBuddy 导出 + 本地 3B，省去 API 调优与重试工程（−8） |
| **人工校验工作台（新增）** | — | **10** | 知识点与依赖边的人工确认/修正 UI（`needs_review` 工作流） |
| **Stage 2 依赖图 ★创新点** | 30 | **28** | 结构线索与 DAG 校验不变；P10 改为离线跑（−2） |
| Stage 3 检索与答疑 | 35 | **33** | −prompt 调优（−4）、+模板库设计（+2） |
| 前端五页 UI | 25 | **25** | 不变 |
| 部署 / 评测 / 交付物 | 17 | **15** | — |
| **合计** | 185 | **185** | |

> 模板库规模从"8–12 个变体"下调为 **5–6 个变体**（6 种 `turn_type` × 5–6 = 30–36 条模板），这已足够避免明显的重复感。

#### 4.8.5 创新点表述的调整（**待项目负责人拍板**）

| | 原表述 | 约束下的推荐表述 |
|---|---|---|
| 主创新点 | 从材料**自动**抽取知识点级依赖图 | **AI 预抽取 + 人工校验**的知识点依赖图构建，并**首次把"教学依赖图必须无环"作为工程不变量强制保证** |

**建议：不要放弃"抽取"这个核心，但要说清边界。** 理由：

1. 我们相对 Marble 的**真正差异**不在于"是否用了 AI"，而在于 **Marble 是静态数据集、不读材料、无法扩展**；我们的管线**能读任意材料并持续产出** —— 这一点即使中间有人工校验环节也成立。
2. 把重心明确移到**环路校验 + 学习路径 + 卡点根因回溯**这套确定性工程上 —— 它在开源侧仍是空白，且**没有任何同类项目在做**。
3. 人工校验不是缺陷：教育场景里纯 AI 抽取本来就不可信，**"AI 干粗活 + 人做裁决"是更专业的产品形态**，而我们的 `needs_review` 机制天然支持它。

#### 4.8.6 可选的强举证：用 Marble 公开数据集验证校验算法

Marble Skill Taxonomy 公开了 **3221 条前置依赖边**（ODbL 1.0 许可，可商用）。可导入我们的系统跑一遍环路校验，并在质量报告页展示结果。

- 若**检出环** → 有力证明"**即使人工标注的图也未必无环，环路校验是必要的**"
- 若**未检出环** → 证明算法不误报，且我们能处理外部数据集导入

约 4 人时，从缓冲里出。**D7 后若有余力再做，不占用主计划。**

#### 4.8.7 必须补做的一件事：向主办方确认规则口径（V-06）

手册原文是：

> "参赛者以 **LearnBuddy 平台为赛事唯一指定平台**，结合大模型能力与 Web 前端等技术的深度融合……"

这句话可以有两种解读：

| 解读 | 含义 |
|---|---|
| 严格 | 作品产品不得调用非 LearnBuddy 的模型 |
| 宽松 | LearnBuddy 是赛事指定的**开发与运营平台**，"结合大模型能力"并未限定哪家大模型 |

**建议 D1 花 30 分钟在赛事群里问清楚**，因为这直接影响工时分配：

- 若**严格禁止** → 按 §4.8 执行，**当前基线不变**（§4.8 已是既成方案）
- 若**其实允许** → 可取回 Qwen-VL（图片解析质量大幅提升）与 DeepSeek，**约省 12 人时**

> **但无论哪种结果，§4.8.3 的运行期零依赖架构都应保留** —— 它本身就是加分项：成本归零、延迟 <200ms、可复现、演示不翻车。

---

## 5. 功能规格

### 5.1 Stage 1 · 素材解析

**输入**：一个或多个素材文件（见 §2.3 S1）
**输出**：① 带页码锚点的 Markdown；② 素材清单；③ 可被 Stage 2 消费的 `material_blocks`
**对应模块**：`backend/app/parse/`

**功能点**

| 编号 | 功能 | 规格 |
|---|---|---|
| F1.1 | 上传 | 拖拽/点选多文件；单文件 ≤ 50MB；支持格式白名单，非白名单给出明确拒绝原因 |
| F1.2 | 类型识别 | 自动判定 `source_type`（含"PDF 是文本版还是扫描版"的探测：抽样页文本字符数 < 阈值判为扫描版） |
| F1.3 | 解析方式可见 | 每份素材在清单里显示实际使用的 `parse_method`（`text_extract` / `multimodal_llm` / `ocr` / `asr`） |
| F1.4 | 统一转 Markdown | 所有类型最终产出 Markdown；每个 block 携带 `page_no` / `line_start` / `line_end` |
| F1.5 | 结构化字段抽取 | 对题目类内容抽取：**题型、题干、选项、答案、所属知识点**（JSON 结构，见 `docs/prompt-contracts.md` P2） |
| F1.6 | 素材清单 | 每批处理完输出：文件名、类型、解析方式、页数/时长、成功/失败、**存疑处列表** |
| F1.7 | 进度与失败可见 | 解析状态轮询可见（pending/parsing/done/failed/partial）；失败可单独重试，不阻塞同批其他文件 |
| F1.8 | Markdown 预览 | 前端可预览解析结果，点击可定位到对应页码 |

**边界与异常**

- 单文件失败不得导致整批失败 → `partial` 状态。
- 加密 PDF / 损坏文件 → 明确报错文案，不留空白页。
- 页数超限（>100 页）→ 提示分批上传，不静默截断。
- **上传音频文件（mp3/wav/m4a）→ 明确拒绝**：`UNSUPPORTED_FORMAT`，文案为"本版本聚焦图文材料解析，暂不支持音频，请上传 PDF / Word / PPT / 图片"。**不显示"即将支持"入口**（§6.1）。

### 5.2 Stage 2 · 知识点结构化

**输入**：`material_blocks`
**输出**：章—节—知识点三级结构（含五要素与溯源）、知识点 DAG、学习路径
**对应模块**：`backend/app/extract/`

**抽取流程（两级，避免上下文超限与结构漂移）**

```
Step 1  目录识别：整份 Markdown → 章/节骨架（P3，JSON 结构化输出）
Step 2  逐节抽取：按"节"为单位分段投喂 → 该节的知识点列表（P4）
        （每个知识点含 name / summary / difficulty / examples /
          misconceptions / source_quote）
        ※ 本步【只抽节点、不抽边】—— 避免边抽取污染知识点粒度
Step 3  归一与去重：跨节合并同名知识点，冲突时保留溯源更完整者

Step 4  依赖构建 ★本项目创新点（完整设计见 docs/innovation.md §5）
        4a 结构线索：章节/节顺序天然给出方向先验（免费，不调模型）
        4b 语义线索：P10 只对【跨节 / 跨章】知识点对做判定
                     → (A, B, hard|soft, reason, confidence)
        4c 融合与冲突标记：语义与结构一致 → 采纳并提升 confidence
                           语义与结构冲突 → 降 confidence + 标 needs_review
                           （★不静默采纳、也不丢弃，而是显式暴露）
        4d DAG 校验：拓扑排序；有环则按 confidence 升序剪边 + WARN 日志

Step 5  入库与索引：写库 + 生成 embedding + 更新 FTS5 索引
```

**功能点**

| 编号 | 功能 | 规格 |
|---|---|---|
| F2.1 | 三级结构 | 结构固定为「章 — 节 — 知识点」，章节号与标题保留材料原貌 |
| F2.2 | 五要素齐全 | 每个知识点必须带：所属章节、难度 1–5、前置知识点、典型例题、常见误区；缺失的字段显式标 `待核实` 而非留空 |
| F2.3 | 难度评估 | 1–5 整数，同时输出 `difficulty_reason`（为什么是这个难度），避免拍脑袋 |
| F2.4 | 前置依赖（★核心创新点） | 分 `hard`（不学懂就学不会）与 `soft`（学了更好懂）两类；**每条边必须带 `reason`**（写清不学懂前置会卡在哪）；抽取采用**结构线索 × 语义线索双通道融合**，见 `docs/innovation.md` §5 |
| F2.5 | 溯源 | 每个知识点必须带 `source_material_id` + `source_page` + `source_quote`（原文片段），前端可一键跳原文 |
| F2.6 | 可查询 | 支持按章节 / 难度区间 / 关键词查询，分页返回（见 `docs/api-spec.md`） |
| F2.7 | 可视化 | 知识图谱视图：节点=知识点（颜色映射难度），边=前置依赖；支持按章节折叠 |
| F2.8 | 学习路径 | 指定目标知识点 → 反向遍历前置依赖 → 拓扑排序输出有序学习路径；无前置的为起点 |
| F2.9 | 可重跑 | 同一素材重跑抽取，结果与上次做 diff 并展示变化（用于验证稳定性） |
| F2.10 | **依赖冲突显式标记 ★** | 语义判定与章节结构矛盾时，**降 `confidence` 并标 `needs_review`，且在前端可见**；不静默采纳、也不丢弃（对应验收项 B1-4） |
| F2.11 | **边溯源** | 每条前置边记录 `reason` 与来源知识点的原文片段，前端点开可跳原文位置 |

**质量红线**

- 三级结构完整率必须 **100%**（不存在"孤儿知识点"）。
- 溯源覆盖率必须 **100%**。
- DAG 环数量必须 **0**。
- 前置边 `reason` 完备率必须 **100%**。

### 5.3 Stage 3 · 交互式答疑辅导

**输入**：学生自然语言提问 + 已入库知识库
**输出**：符合苏格拉底节奏的引导式回复 + 每轮三件产出
**对应模块**：`backend/app/tutor/`

**核心：苏格拉底式状态机（显式、可测，不是"尽量引导"）**

```
                    ┌──────────────────────────────────┐
                    │  S0 检索与越界判定                 │
                    └──────────────┬───────────────────┘
                          越界 ────┴──── 命中
                          │                │
                  ┌───────▼──────┐  ┌──────▼───────────────────────┐
                  │ REFUSE       │  │ S1 PROBE  反问答疑            │
                  │ 明说材料里没有 │  └──────┬───────────────────────┘
                  │ + 最接近知识点 │         │
                  └──────────────┘   学生答对 ─┴─ 答错/答不上
                                          │            │
                                  ┌───────▼──┐  ┌──────▼────────┐
                                  │ CONFIRM  │  │ S2 HINT1 提示  │
                                  │ 确认+巩固 │  └──────┬────────┘
                                  └──────────┘         │ 仍答不上
                                                ┌───────▼────────┐
                                                │ S3 HINT2 强提示 │
                                                └───────┬────────┘
                                                        │ 第 2 次答不上
                                                ┌───────▼────────┐
                                                │ S4 EXPLAIN     │
                                                │ 直接完整讲解    │
                                                └────────────────┘
```

**硬性规则**

| 编号 | 规则 |
|---|---|
| R1 | 每次提问**必须先检索再回答**，禁止无检索直接生成 |
| R2 | 首轮**绝不给答案**，只给反问（S1 PROBE）。这是产品的核心性格 |
| R3 | 学生**连续 2 次**答不上/答错 → 强制进入 S4 直接讲解，不得继续追问 |
| R4 | 检索不到 → 必须走 REFUSE，回复中不得包含任何材料外的知识断言 |
| R5 | 每轮结束必须输出三件事：**涉及的知识点** / **学生卡在哪一步** / **下一步建议练习** |
| R6 | 学生的每个"卡点"判定必须给出依据（引用哪个知识点的哪个误区 / 哪个前置没掌握） |
| R7 | 流式输出（SSE），首 token ≤ 3s |

**功能点**

| 编号 | 功能 | 规格 |
|---|---|---|
| F3.1 | 会话创建 | 可指定答疑范围（全部材料 / 选定材料），范围影响检索域 |
| F3.2 | 提问与流式回复 | SSE 流式；回复中嵌「溯源卡片」组件 |
| F3.3 | 溯源卡片 | 展示：知识点名 + 材料名 + 页码 + 原文片段；可点击跳转到原文位置 |
| F3.4 | 诊断面板 | 右侧固定面板实时显示：涉及知识点、卡点、下一步练习（本轮 R5 的三件事） |
| F3.5 | 降级提示 | 进入 S4 时 UI 明确提示"连续两次没答上，我直接讲" —— 让学生感知到系统的规则，而不是觉得被放弃 |
| F3.6 | 拒答提示 | REFUSE 时展示「材料里没有」，并列出最接近的 2–3 个已入库知识点作为替代入口 |
| F3.7 | 会话记录 | 保存全部轮次（含 `turn_type`、检索依据、诊断结果），供复盘与评审参考 |
| F3.8 | **卡点根因回溯 ★** | 学生在知识点 K 卡住时，沿 `kp_prerequisites` 的 hard 边**反向可达**，结合命中的误区，定位最可能的断层前置知识点 Pk；输出"你这一题卡在 K，但根因更可能是 Pk 没吃透（材料第 X 页）"。这是相对同类项目的实质差异（`docs/innovation.md` §5.3） |

### 5.4 素材清单与导出

| 编号 | 功能 | 规格 |
|---|---|---|
| F4.1 | 素材清单表 | 字段：文件名、类型、大小、解析方式、页数、状态、存疑处 |
| F4.2 | 知识点导出 | 导出 JSON / CSV（含全部字段与溯源），便于外部核对 |
| F4.3 | 质量报告页 | 展示本次会话的解析成功率、字段完备率、溯源覆盖率、越界拒答次数 —— **把工程严谨做成可见的一页** |

### 5.5 LearnBuddy 使用留痕（对应 25% 的「AI 工具使用」得分）

这不是开发任务，是**工作方式**，但要刻意做：

- 需求讨论、方案取舍、Prompt 迭代、Bug 定位，全部在 LearnBuddy 里进行（就像现在这样）。
- 每天结束导出一份当天的关键对话片段，归档到 `docs/buddy-logs/YYYY-MM-DD.md`，注明：本日用 LearnBuddy 解决了什么问题、产出了什么。
- D10 整理成提交用的「LearnBuddy 使用记录」，附索引表。

### 5.6 加分项执行

> **⚠️ 重要变化（v1.2 最终确认）**：团队三人**同属计算机与软件学院、人工智能专业**，不满足手册「团队成员来自 2 个及以上不同专业/院系」的条件，**+2 跨专业加分确定无法获得，已放弃，不再投入任何时间与精力**。

| 加分项 | 分值 | 状态 | 执行方式 | 责任人 | 截止 |
|---|---|---|---|---|---|
| 跨专业组队 | +2 | **❌ 已确认不满足，放弃** | 不再做任何确认或申报动作。**PPT 中禁止出现任何暗示团队专业多样性的表述** | — | — |
| 社交媒体传播 | +1 | ✅ 可争取 | 按手册要求"过程设计完整"：分 3–4 篇连载（Day2 选题与架构 / Day5 首个可用版本 / Day8 依赖图抽取设计 / Day10 复盘），每篇含真实截图与踩坑，**不是几句话+配图** | 指定 1 人 | D10 前发布 |

**丢失 2 分后的补偿策略（必须执行，不是建议）**

+2 是白送的分，丢了只能从主评分项里挣回来。因此：

1. **把 §1.5 的主创新点做实到可验证**：教学依赖 DAG 的四个子能力（自动抽取 / 边带理由与溯源 / 环路校验 / 卡点根因回溯）必须全部做出来，并在 Demo 视频里被看清 —— 这是"技术创新性 30%"的主要抓手。
2. **「AI 工具使用 25%」做到无懈可击**：LearnBuddy 对话记录要成体系（每日归档 + 索引表），这是纯执行力得分，不能丢。
3. **不靠"团队多样性"叙事**：改为"**同一专业、三种工程角色分工**"（解析工程 / 数据与检索 / 前端与交互），把分工清晰度与接口约定做出来。**避免任何会被当场戳破的表述。**
4. **教学内容的外部背书**（可选，不参与加分）：找一位有教学经验的老师做一次 30 分钟的知识点体系评审，在 PPT 里以"教学内容评审"致谢形式呈现。**注意：这是质量背书，不是跨专业加分，不要在提交材料里暗示它满足加分条件。**

---

## 6. 关键设计决策与取舍记录

> 已确认的决策（2026-09-16 由项目负责人拍板；v1.1 补齐 D-07 ~ D-11）：

| 编号 | 决策点 | 结论 | 理由 | 代价 / 缓解 |
|---|---|---|---|---|
| D-01 | 架构形态 | **前后端分离 + 数据库** | 工程完整性 25% 是硬分，架构要经得起看源码 | 10 天风险偏高 → 用 SQLite 免运维、单容器部署、D5 强制首次上线来压缩风险 |
| D-02 | 模型能力来源 | **自备大模型 API** | 可控性最高，能精细调 prompt 与结构化输出，不受平台接口开放情况制约 | 少量费用自担；通过 Provider 抽象层保留换模型的能力 |
| D-03 | 范围优先级 | **三阶段全打通，Stage 2 做深** | 端到端可演示才拿得到分；知识点结构化 + 学习路径是最强差异化 | Stage 3 交互做扎实不做花，砍掉一切非必要动效 |
| D-04 | 检索实现 | SQLite + NumPy 暴力余弦 + FTS5 混合 | 知识点千级规模，暴力检索 <10ms；引入向量库是纯负债 | 规模上到 10 万级需重构（初赛不可能触及） |
| D-05 | 异步任务 | FastAPI BackgroundTasks + 任务表 | 不引入 Celery/Redis，少两个部署单元 | 单机并发有限；Demo 场景够用 |
| D-06 | 单容器部署 | 前后端代码分离，运行时同容器 | 一个公网 URL、无 CORS、部署成本最低 | 需在 README 里说明这是刻意的部署取舍，不是架构混乱 |
| D-07 | 目标学科 | **计算机网络**，做深第 4 章（网络层）与第 5 章（传输层） | 依赖链真实存在、误区经典、例题可判对错、材料易得（详见 §2.6） | 评委是计算机专业，抽错会被一眼看穿 —— 只能靠 E-1 金标准评测集硬碰 |
| D-08 | 音频转写 | **不做**（v1.2 从"窄通道"改为"完全不做"） | 权衡后判定：收益（多模态完整性）不足以抵消风险（术语错误沿链路传播、直接击穿幻觉率红线）与工时占用；省下的时间投给主创新点更划算 | 赛题字面写"多模态"，答辩可能被问及 → **必须在 PPT 范围说明里如实交代取舍**，不能假装没想到（详见 §6.1） |
| D-09 | 模型分工（**v1.4 已暂停**） | ~~视觉走 Qwen-VL，文本走 DeepSeek~~ → 改为 **LearnBuddy（主）+ 本地 3B（兜底）+ PaddleOCR** | 赛事规则不允许产品调用第三方 API（见 D-13）；原选型的技术理由（文本模型不能读图）依然成立，只是执行载体变了 | 图片解析质量下降 → 3 张图片人工校对兜底（§4.8.2）；路由表保留但不启用，规则若放宽可一行切回 |
| D-10 | 是否依赖 LearnBuddy API | **不依赖**。产品内全部走自备 API；LearnBuddy 以"对话留痕"方式举证「AI 工具使用」得分 | 平台接口开放情况不确定，把不确定性从关键路径上摘掉 | 需额外做每日对话归档（§5.5），但这是纯执行力工作，可控 |
| D-11 | 主机与预算 | 云服务器轻量档位 + 模型按量付费，**总预算 100 元** | 见 §6.2 预算表，实测可控制在 100 元内 | 若拿不到新用户/学生优惠，主机费用会翻倍 → **D1 第一件事就是做学生认证** |
| D-12 | **创新点** | **从材料自动抽取「知识点级、带硬/软语义、带理由与溯源、且通过 DAG 环路校验」的前置依赖图**（§1.5） | 调研确认开源侧空白：DeepTutor 无依赖图、Marble 靠人工标注、学术方法（AAAI 2019 / BEA 2023）未工程化 | 依赖 LLM 抽边的质量 → 有降级预案（退化为仅结构边）；验收指标 B1-1 ~ B1-6 |
| D-13 | **赛事不允许调用第三方 API（已触发）** | **离线构建 + 在线确定性推理**，配合**三层抽取策略**（LearnBuddy 主 / 本地 3B 兜底 / 人工校验），见 §4.8 | 主创新点（DAG 环路校验、学习路径、根因回溯）与苏格拉底状态机**本身就无需 LLM 在线运行**；摘出关键路径后成本归零、延迟 <200ms、演示不看外部服务脸色 | 回复自然度下降、图片解析质量下降 → 模板库 + 3 张图片人工校对缓解；工时已重排（§4.8.4），总额仍为 185 人时 |
| D-14 | **官方规则口径待核实** | 手册"唯一指定平台"存在**严格 / 宽松**两种解读，D1 须向主办方确认（§4.8.7） | 若规则其实允许第三方 API，可取回 Qwen-VL 与 DeepSeek，**约省 12 人时** | 无论结果如何，§4.8.3 运行期零依赖架构都保留（它本身是加分项） |
| D-15 | **创新点表述（已定稿）** | **AI 预抽取 + 人工校验**的依赖图构建，并**首次把「教学依赖图必须无环」作为工程不变量强制保证**（§4.8.5、§1.5） | 我们相对 Marble 的真正差异是"能读任意材料且可扩展"，而非"是否用了 AI"；重心移到环路校验 + 学习路径 + 根因回溯 | 答辩口径：说"AI 干粗活 + 人做裁决"，并主动补"人工校验是更专业的产品形态" |
| D-16 | **作品形态与部署（v1.9 定稿）** | **混合形态**：LearnBuddy **专家智能体**（零门槛入口）+ **自建 Web Demo**（技术深度）；**做轻量部署、提供稳定可访问的链接**，但不做生产级环境（无域名备案 / 运维 / SLA） | 手册 §二 明确「部署至浏览器环境 + 可直接访问的在线链接」是**必需材料**，评审按手册走；赛事方口头口径只**降低门槛、不取消要求**；双形态互为保险 | 硬边界四条：① 核心算法必须有代码承载 ② **源码仓库 + Demo 视频不可省** ③ 代码层强制降级的主实现放 Web Demo ④ **链接必须常驻可访问，不允许用临时隧道糊弄** |

### 6.1 音频转写：为什么最终不做（D-08 决策记录）

**曾经的候选方案**：单文件上传 → ASR（挂领域热词表）→ 带时间戳 Markdown → P9 术语归一化 → 复用既有 Stage 2/3 管线。

**权衡结果：不做。**（v1.2 最终判定，推翻 v1.1 的"窄通道"方案）

| 维度 | 支持做的理由 | 反对做的理由（权重更高） |
|---|---|---|
| 赛题契合 | 赛题字面写"多模态"，缺音频只剩"图文"，答辩可能被问 | — |
| 真实场景 | 讲课录音、录播课是常见素材形态 | — |
| 技术成本 | 转写后复用现成管线，边际成本低 | 仍需 上传→转码→ASR→分段→时间戳对齐→归一化，约半天到 1 天 |
| **质量风险** | — | 🔴 **决定性因素**：计算机网络术语密集且多为英文缩写（TCP、CSMA/CD、ARP、ICMP、OSPF、CIDR、慢启动…）。ASR 错识别会沿链路传播：Markdown 错 → 知识点错 → **溯源引用的"原文"本身就是错的** → 直接击穿 A3-3「幻觉率必须为 0」这条红线 |
| 演示性价比 | — | Demo 视频里音频"看不出效果、只能听"，3 分钟里不划算 |
| 工时机会成本 | — | 🔴 省下的半天到 1 天投给 §1.5 的主创新点（教学依赖 DAG），边际收益明显更高 |

**决策后的必然动作（不可省略）**

1. **UI 层**：上传音频 → 明确拒绝并给出可读原因（见 §5.1 边界与异常），**不显示"即将支持"入口**。
2. **PPT 层**：在"作品范围与取舍"一页**如实写明"音频转写经评估后主动放弃，原因是……"**。主动交代取舍比被评委问倒强，而且这本身就是工程判断力的体现。
3. **文档层**：`docs/prompt-contracts.md` 中的 P9（音频术语归一化）已随之删除，不留孤立契约。

> **答辩标准回答（建议背下来）**：「我们评估过音频转写，方案都设计到窄通道级别了。最终放弃是因为这门课的术语几乎全是英文缩写，ASR 错识别会一路传到我们溯源引用的原文里，直接破坏最核心的'基于材料、可溯源'这条线。与其做一个会污染可信度的功能，我们选择砍掉它，把时间投到知识依赖图上。」

### 6.2 成本预算与成本控制（100 元总预算）

**预算分配表**

| 项 | 方案 | 预估费用 | 说明 |
|---|---|---|---|
| **云服务器** | 轻量应用服务器 2 核 2G / 50G 盘（**v1.9 恢复购买**） | **¥0 ~ ¥68** | 手册要求提供**可直接访问的在线链接** → 必须有常驻主机。**先做高校学生实名认证领券，大概率压到 ¥0** |
| 域名（可选） | 新客 cn 域名 | **¥0 ~ ¥1** | 不买也能用 IP 访问；只为链接好看 |
| **模型费用** | LearnBuddy 平台（赛事提供 2500 Credits）+ 本地 3B + PaddleOCR（本地） | **¥0** | 赛事规则不允许调用第三方 API，全部为平台能力 + 本地开源方案 |
| 兜底备用 | — | **¥10** | 留给极少数意外 |
| **合计** | | **¥0 ~ ¥79** | 学生券后很可能仍是 **¥0 ~ ¥10**（v1.8 曾误判为"不买主机"，见 §12 变更日志） |

**两条能立刻省下钱的动作（D1 必须做）**

1. **先做高校学生实名认证再付钱**。云厂商对认证学生有 300 元无门槛代金券一类的权益，可能直接让主机费用归零。**顺序很关键：先认证领券，再下单买主机，别反过来。**
2. **警惕 LearnBuddy Credits 的消耗节奏**（v1.4）。抽取走平台后，2500 Credits 是唯一的"模型预算"。**先在小材料上跑通 Prompt 再上大批量**，避免在调 Prompt 阶段把额度烧掉。同时用本地 3B 承接调参类试验（本地不计 Credit）。

> **v1.4 的成本结构已经变了**：模型费用归零，**服务器几乎成了唯一开销**。这意味着预算是本轮约束里最不成问题的一项 —— 需要省的已经不再是钱，而是 **Credits 与工时**。

**成本控制策略（写进实现，不是口号）**

| # | 措施 | 省在哪 |
|---|---|---|
| C1 | **调 Prompt 阶段的试验一律走本地 3B 模型**，只在产出最终结果时才用 LearnBuddy | 调 Prompt 的调用量占总量 80%+，这一条省最多 Credit |
| C2 | **抽取结果落库复用**：素材按文件 hash 建唯一索引，同一文件重复上传不重复抽取 | 避免改前端时反复重跑解析、白烧 Credits |
| C3 | **文本版 PDF 走 PyMuPDF 免费抽取**，只对扫描页/图片调用视觉模型 | 视觉调用是最贵的一档，绝不能无差别使用 |
| C4 | **图片先降采样**（长边压到 1600px）再送模型 | 控制图像 token 数量 |
| C5 | **LLM 测试用 fixture**：把已跑通的 LLM 响应固化成测试夹具，单测不真调 API | 单测反复运行不花钱，且更快 |
| C6 | **`llm_calls` 表每日核对 token 消耗**，设日预算告警阈值 | 早发现异常放大（程序死循环重试是烧钱黑洞） |
| C7 | **Prompt 精简**：P4 只投喂当前"节"的 blocks，不投喂整篇 | 上下文长度直接决定成本 |

**超支应对顺序**：先关闭非必要的视觉调用（保留 C3 之外的一律砍）→ 再压缩评测集规模 → 最后才考虑申请追加预算。**服务器和文本 API 是底线，不能砍。**

---

## 7. 验收标准

**所有指标必须可测。达不到即为未完成，不得用"基本实现"模糊化。**

### 7.1 Stage 1 · 解析

| 编号 | 指标 | 目标 | 验证方式 |
|---|---|---|---|
| A1-1 | 支持素材类型数 | ≥ 5 类（PDF文本版 / PDF扫描版 / DOCX / PPTX / 图片） | 各类型各 1 份实测 |
| A1-2 | 文本版 PDF 原文抽取准确率 | ≥ 98% | 与人工校对文本字符级对比 |
| A1-3 | 扫描件/图片关键字段抽取准确率 | ≥ 85% | 20 题样本，核对题型/题干/选项/答案 |
| A1-4 | 素材清单完整性 | 100% 素材带"解析方式 + 存疑处"字段 | 逐份核对 |
| A1-5 | 单份 ≤20 页材料解析耗时 | ≤ 90s | 计时 |
| A1-6 | Markdown 页码锚点 | 100% 的 block 可定位到页码 | 抽样 30 个 block 点击验证 |
| A1-7 | 单文件失败隔离 | 单文件失败不影响同批其他文件 | 故意投毒 1 份损坏文件 |

### 7.2 Stage 2 · 结构化

| 编号 | 指标 | 目标 | 验证方式 |
|---|---|---|---|
| A2-1 | 三级结构完整率 | 100% | SQL 校验：无 section_id 为空的 kp |
| A2-2 | 五要素字段完备率 | ≥ 95% | SQL 校验非空率 |
| A2-3 | 溯源覆盖率 | 100% | SQL 校验 source_page / source_quote 非空 |
| A2-4 | DAG 环数量 | 0 | 图算法校验 |
| A2-5 | 知识点命名与归属正确率 | ≥ 85% | 人工抽检 30 个知识点 |
| A2-6 | 学习路径可生成 | 100% 的目标知识点都能出路径且拓扑有序 | 抽样 10 个验证 |
| A2-7 | 同输入可复现 | 重跑两次，知识点集合差异 ≤ 10% | 跑两遍 diff |

### 7.3 Stage 3 · 答疑

| 编号 | 指标 | 目标 | 验证方式 |
|---|---|---|---|
| A3-1 | 检索命中率 | ≥ 85% | 30 个基于材料的问题，人工判定是否命中正确知识点 |
| A3-2 | 越界拒答准确率 | ≥ 90% | 20 个材料外问题，检查是否正确拒答 |
| A3-3 | **幻觉率** | **0**（出现即视为严重缺陷） | 人工审查全部回答中的事实断言是否均有溯源支撑 |
| A3-4 | 首轮不给答案 | 100% | 检查所有首轮 `turn_type` 均为 `probe` |
| A3-5 | 连错 2 次降级 | 100% | 构造脚本走完整状态机 |
| A3-6 | 三件产出完整率 | 100% | 每轮诊断 JSON 字段非空 |
| A3-7 | 首 token 延迟 | ≤ 3s | 压测 20 次取 P95 |

### 7.4 工程与交付

| 编号 | 指标 | 目标 |
|---|---|---|
| A4-1 | 后端核心 API 单测覆盖率 | ≥ 60%（解析、抽取校验、检索、状态机为重点） |
| A4-2 | 前端控制台 | 无 error 级报错 |
| A4-3 | 一键启动 | `docker compose up` 或 `make dev` 可跑通，README 步骤实测可用 |
| A4-4 | 仓库规范 | README + LICENSE（开源协议）+ 清晰目录 + 有意义的 commit 历史 |
| A4-5 | 公网链接 | 可访问，且**不依赖本地环境**（换台电脑打开能用） |
| A4-6 | 交付物齐备 | 链接 / 源码 / Demo 视频（3min）/ PPT / LearnBuddy 记录，五项全有 |
| A4-7 | 预置演示材料 | 内置 1 套示例材料，评委打开即可体验完整流程（不依赖现场上传） |

### 7.5 创新点专项验收（B1-*，v1.2 新增）

> 对应 §1.5 的主创新点（教学依赖 DAG）。这是"技术创新性 30%"的核心举证材料，**必须单独跑测并留有记录**。

| 编号 | 指标 | 目标 | 验证方式 |
|---|---|---|---|
| B1-1 | 前置依赖边人工抽检合理率 | ≥ 80% | 从抽出的边中随机抽 50 条，人工判定"这条前置关系在教学上是否成立" |
| B1-2 | **DAG 环数量** | **0** | 图算法校验（拓扑排序） |
| B1-3 | 学习路径拓扑有序率 | 100% | 抽 10 个目标知识点，检查路径中任一节点都不依赖其后面的节点 |
| B1-4 | **结构-语义冲突检出率** | **100%** | 构造 3 组"故意让语义判定与章节顺序矛盾"的对抗性测试输入，检查是否**全部被标为 `needs_review`**，而不是静默采纳。**⚠️ 夹具状态：一致性检查类夹具已就绪**（`samples/graph-infer-tests/T1-single-cycle.json`、`T2-adversarial-composite.json`），冲突标记类待补 3 组 |
| B1-5 | 前置边 `reason` 完备率 | 100% | SQL 校验非空率 |
| B1-6 | 卡点根因回溯可用率 | ≥ 70% | 抽 20 个"停在中后段知识点"的卡点场景，人工判定回溯出的前置根因是否合理 |

> **B1-4 是这套设计里最能体现工程深度的一项**：它验证的不是"模型抽得准不准"，而是"**系统在模型不确定时是否诚实**"。构造对抗性输入、并让评委看到你的系统会显式暴露矛盾，比单纯报一个准确率更有说服力。

---

## 8. 排期与里程碑

**可用开发时间：9/16 – 9/25（10 天），9/26 为提交日（不安排开发）。**

> **⚠️ 排期偏移（v2.1 登记）**：9/16 – 9/17 的绝大部分时间用于方案论证与规格撰写（产出 SPEC v1.0 → v2.0 与 10 份配套文档），**实际开发从 9/17 开始**。因此**整体偏移 1 天**：可用整天约 **9 天**（原 10 天）。
>
> **本次不上调目标** —— 继续按原排期的任务内容执行，把这一天记在 **R-01（时间不够）** 名下，**在 D4 EOD 复核时统一决定从哪里补**（优先从 §5.4 辅助差异化的 A2 / A3 里砍，创新点 28 人时不动）。
>
> **登记理由**：规格先行的收益是实实在在的（至少避免了三处大返工：音频链路、正式部署假设、创新点定位），但**收益不记进工时，成本要记**。不登记就会变成"感觉一直在忙却不知道进度差在哪"。

### 8.1 工时预算（V-13 已闭环）

| 项 | 数值 |
|---|---|
| 人数 | 3 人 |
| 每人每天 | 8 小时 |
| 每天合计 | **24 人时** |
| 10 天总计 | **240 人时** |
| 每日固定开销（站会 0.5h + LearnBuddy 归档 0.25h）× 3 人 × 10 天 | ≈ 22 人时 |
| 技术风险与调试缓冲（15%） | ≈ 33 人时 |
| **可用于功能开发的实际工时** | **≈ 185 人时** |

**模块工时分配（185 人时）**

| 模块 | v1.6 | **v1.9** | 变化原因 |
|---|---|---|---|
| Stage 1 素材解析（OCR 链路、存疑处、素材清单） | 44 | **42** | −2：图片素材仅 3 张，OCR 版式后处理可简化 |
| Stage 2 知识点抽取（三级结构 + 五要素 + 溯源） | 30 | **30** | — |
| 人工校验工作台 | 10 | **8** | −2：简化为"列表 + 行内编辑"，不做完整工作流 |
| **Stage 2 依赖图（双通道融合 + DAG 校验）★创新点** | 28 | **28** | — |
| Stage 3 检索与答疑（状态机 + 模板库 + 根因回溯） | 33 | **33** | — |
| 前端五页 UI 与可视化 | 25 | **25** | — |
| **LearnBuddy 专家智能体** | — | **5** | 角色设定、知识库接入、对话策略、测试（v1.9 由 7 下调，因链接已由 Web Demo 承担） |
| **部署（轻量主机 + 单容器 + 稳定性保障）** | 6 | **6** | **v1.9 恢复**（v1.8 曾砍到 3）。手册要求在线链接，必须有常驻部署 |
| 评测跑测 | 4 | **4** | — |
| 交付物（Demo 视频 / PPT / 文档 / 社媒） | 5 | **4** | −1 |
| **合计** | 185 | **185** | v1.9 从专家智能体与交付物各匀出工时补回部署 |

> **创新点独占 28 人时（15%），这是刻意的**：它对应权重最大的「技术创新性 30%」，不能当顺带做的东西。**若某天进度落后，砍的是别处，不是这里。**

### 8.2 十日排期

| 天 | 日期 | 主线任务 | 里程碑（当日 EOD 必须达成） |
|---|---|---|---|
| D1 | 9/16 | 仓库与脚手架；数据库建表；抽取辅助通道打通（LearnBuddy 导出流程 + 本地 3B）；PaddleOCR 环境搭建；前端骨架与路由；**云服务器学生认证与选购**（先领券再下单）；**向主办方确认规则口径（V-16）** | **M1 骨架跑通**：前端能调通后端，数据库有表，抽取通道能返回结果 |
| D2 | 9/17 | Stage 1：PDF（文本版）与 DOCX 解析 → Markdown（带页锚点）；素材上传与状态轮询；**确认 §2.6 知识骨架并锁定材料清单** | — |
| D3 | 9/18 | Stage 1：图片/扫描页 **PaddleOCR 本地解析 + 人工校对**；PPT 解析；结构化字段抽取；素材清单 | **M2 解析可用**：上传 PDF+板书照片能出 Markdown 与素材清单 |
| D4 | 9/19 | Stage 1 收口：存疑处标注、失败隔离、重试、解析质量报告页；**评测集 E-1/E-2/E-3 完成** | Stage 1 达到 A1-* 指标 |
| D5 | 9/20 | **轻量部署上线**（单容器 + 公网 IP，不做域名备案等正式环境）；Stage 2 打底：章节目录识别 | **M3 有稳定可访问的链接** —— 手册必需材料，**硬要求不推迟**（门槛比原计划低：无需正式环境） |
| D6 | 9/21 | Stage 2：逐节知识点抽取（五要素）+ JSON 校验 + 入库 + embedding | — |
| D7 | 9/22 | **★创新点主体**：依赖边抽取（P10 语义判定）+ 结构线索 + 冲突标记 + DAG 校验与剪枝；知识图谱可视化；学习路径拓扑排序；混合检索层 | **M4 结构化可用**（达 A2-*）+ **B1-2 / B1-5 达标**（环数 0、reason 完备） |
| D8 | 9/23 | Stage 3：检索 + 越界判定 + 苏格拉底状态机 + 三件产出；**★卡点根因回溯（F3.8）**；前端答疑页与诊断面板；**LearnBuddy 专家智能体配置启动**（角色设定 + 知识库接入） | — |
| D9 | 9/24 | **★专家智能体上线并与 Web Demo 入口互指**；端到端联调；跑全部验收指标（含 **B1-1 ~ B1-6 创新点专项**，特别是 B1-4 对抗性测试）；修缺陷；**功能冻结** | **M5 端到端闭环**（达 A3-* + B1-*）+ **专家智能体可交互体验** |
| D10 | 9/25 | 体验打磨；Demo 视频脚本与录制；介绍 PPT；README 与架构文档；LearnBuddy 记录整理；仓库清洁 | **M6 交付物齐备**（达 A4-*） |
| — | 9/26 | 提交（**上午完成，不拖到 23:59**） | 已提交 |

> **★ 标记的是本项目的核心创新工作**（§1.5）。进度落后时，**优先砍别处，不许先砍这里** —— 它对应「技术创新性 30%」这个最大权重项。
>
> **缓冲项**：v1.2 起音频链路已整条移除，当前**无缓冲项**。若需要缓冲，从 §5.4 的辅助差异化（A2 状态机可视化 / A3 存疑处 UI）里砍。

**排期纪律**
- **功能冻结线 = D9 EOD**。之后只允许修 bug 和改文案，不允许加功能。
- **部署不推迟**（v1.9 恢复）：D5 必须把稳定链接跑出来 —— 手册明确"可直接访问的在线链接"是**必需材料**。之后每次实质改动都重新部署，避免"D10 才发现链接打不开"。
- 每天 15 分钟站会（三点：昨天完成/今天计划/阻塞项），阻塞项当天升级。
- 每个里程碑都是**可演示的**，不能演示等于没达成。

---

## 9. 团队分工

**前提**：三人同属计算机与软件学院 · 人工智能专业，**不构成跨专业组队**（§5.6）。分工按**工程角色纵切**，每人端到端负责一条链路。
工时基准：**3 人 × 8 小时 × 10 天**，扣除固定开销与缓冲后**可规划 185 人时**（§8.1）。

> **PPT 里的叙事是"同一专业、三种工程角色分工"**，不要出现"我们来自不同专业"这类会被当场戳破的表述。

### 9.1 角色、模块边界与工时

| 角色 | 代号 | **负责目录（文件级归属，避免合并冲突）** | 主要交付 | 关键指标 | 人时 |
|---|---|---|---|---|---|
| **解析工程** | **P1** | `backend/app/parse/`<br>`backend/app/llm/`（Provider 抽象层，保留不启用）<br>**`pipeline/graph_infer.py`（★核心算法，v2.0 新增）**<br>`scripts/ocr/`、`scripts/ingest.py`<br>`deploy/`、`Dockerfile` | PDF/Word/PPT 文本抽取 → Markdown（带块锚点）；PaddleOCR 集成与版式重建；抽取结果入库脚本；**DAG 拓扑校验与剪枝算法**；Dockerfile 与每日部署 | A1-*、A4-3、A4-5、**B1-2** | 59 |
| **数据与结构化** | **P2** | `backend/app/models/`、`migrations/`<br>`backend/app/schemas/`<br>`backend/app/extract/`<br>`backend/app/retrieve/`<br>`backend/app/api/materials.py`、`knowledge.py`<br>`backend/tests/`、`scripts/evaluate.py` | 数据模型 + Alembic 迁移；知识点抽取流水线；**P10 依赖边判定 + 融合与冲突标记**；混合检索层；接口与单测；**LearnBuddy 抽取的执行与 Prompt 维护**；评测集 E-1 组织 + 跑测脚本 | A2-*、A3-1、A4-1、**B1-1 / B1-4 / B1-5** | 66 |
| **前端与交互** | **P3** | `frontend/`（全部）<br>`backend/app/tutor/`<br>`backend/app/api/qa.py` | 五页 UI；**苏格拉底状态机后端 + 模板库**；**诊断面板与卡点根因回溯可视化**；**人工校验工作台 UI**；图谱可视化；Demo 视频 + PPT + 社媒连载 | A3-2 ~ A3-6、A4-2、A4-6、**B1-3 / B1-6** | 60 |

**归属纪律（硬性）**

1. **不碰别人的目录。** 需要改动别人负责的文件时，先说一声，由 owner 改 —— 直接改会让两人同时改同一文件。
2. **共享文件只有两个**：`README.md` 与 `SPEC.md`。改这两个必须先在群里知会。
3. **API 层按模块切**：`api/materials.py` + `api/knowledge.py` 归 P2，`api/qa.py` 归 P3 —— 避免多人改同一个文件。
4. **前后端只通过接口通信**：前端不得直连数据库、不得调后端内部函数。所有交互走 `docs/api-spec.md` 定义的端点。
5. **P2 比 P1 / P3 多 6 人时**，因为依赖图抽取的主创压在他身上；P1 在 D7 投入 12 人时支援（结构线索与 DAG 校验），P3 负责把结果做成评委看得见的东西。

### 9.2 接口约定（谁给谁什么、何时）

| 编号 | 交付方 → 接收方 | 内容 | 格式 | 冻结时间 |
|---|---|---|---|---|
| I-1 | P1 → P2 | 解析产物 `material_blocks`（含块锚点与 `section` 归属） | 数据库表 | D3 EOD |
| I-2 | P1 → P2 | 抽取结果入库脚本 `scripts/ingest.py` | CLI | D4 EOD |
| I-3 | P2 → P3 | 全部 API 端点 | `docs/api-spec.md` + `/docs` OpenAPI | **D1 EOD 冻结骨架** |
| I-4 | P2 → P3 | 知识点与依赖图查询接口的返回结构 | JSON | D6 EOD |
| I-5 | P1 → P3 | PaddleOCR 的人工校对辅助工具（导出待校对清单） | CLI / JSON | D3 EOD |
| I-6 | P3 → P2 | 前端所需的额外字段需求 | 提 issue 说明 | 随时，但 **D9 后不再接受** |

> **接口冻结纪律**：`docs/api-spec.md` 的端点路径与请求/响应结构在 **D1 EOD 冻结**。之后只允许「新增端点」与「新增可选字段」，**不允许改已有字段名或类型** —— 否则前端要返工。

### 9.3 协作纪律

| 项 | 规则 |
|---|---|
| Git 分支 | `main` 为保护分支；每人 `feat/p1-parse` / `feat/p2-extract` / `feat/p3-frontend`；**每日至少合并一次到 main** |
| 合并前 | 至少 1 人看过 diff |
| 提交信息 | `[P1] 完成 PDF 文本抽取` 形式，前缀标明归属 |
| 每日站会 | **09:00，15 分钟**，固定三问：昨天完成什么 / 今天做什么 / 卡在哪 |
| 阻塞升级 | **卡超过 2 小时立即在群里说**，不要自己硬扛到晚上 |
| 接口变更 | 必须先改 `docs/api-spec.md`，再改代码（规格先行，§12 变更流程） |
| 每日部署 | D5 起，**每天收工前部署一次**，保持公网链接始终可用 |

### 9.4 抽取操作的执行分配（v1.5 新增 —— **最容易漏掉的一块**）

抽取是**离线人工搬运**的（LearnBuddy 主通道，§4.8.1）。这条流水线必须明确到人，否则会被当成"顺手做的事"而漏掉：

| 步骤 | 责任人 | 产出 | 时限 |
|---|---|---|---|
| 1. 把材料按"节"切成带 `block_id` 的片段 | **P1**（解析是他的模块） | 节级输入片段 | D5–D6 |
| 2. 在 LearnBuddy 里跑 P3 → P4 → P10，导出结果 | **P2**（他最懂 schema） | `P4` / `P10` JSON | D6–D7 |
| 3. 跑入库脚本、schema 校验、写库、DAG 校验 | **P2** | 可查询的知识库 | D6–D7 |
| 4. 在工作台上确认/修正知识点与边 | **P3**（工作台是他的模块） | `needs_review` 清零 | D7–D8 |
| 5. **抽查 20%** 的校验结果，防止校验走过场 | **P1**（**交叉校验，避免同源偏差**） | 抽检记录 | D8 |

> **为什么必须交叉**：如果同一个人既抽取又校验，他只会看到自己认可的内容。**由不同的人做校验与抽检，是质量控制的底线。**
>
> **B1-1 的 50 条边抽检同理**：三人各判约 17 条，再交叉复核其中 5 条。判定口径见 [`docs/edge-review-consensus.md`](docs/edge-review-consensus.md)。

### 9.5 逐日任务分配（每人每天 8 小时）

| 天 | P1 · 解析工程 | P2 · 数据与结构化 | P3 · 前端与交互 |
|---|---|---|---|
| **D1**<br>9/16 | 云服务器**学生认证→领券→下单**；部署环境；**PaddleOCR 环境搭建**；**抽取通道跑通**（LearnBuddy 导出流程 + 本地 3B）；**下载教材章节 + 自造 M-C 扫描件 / M-D 板书照片** | 数据模型建表（Alembic）+ SQLAlchemy 模型；API 脚手架；`/health`、`/meta/capabilities` | 前端脚手架（Vite + TS + Tailwind）；路由；设计 token；上传组件骨架 |
| **D2**<br>9/17 | PDF 文本版（PyMuPDF）+ DOCX 解析 → Markdown（带页锚点）；**教材章节按"节"切分并生成 `block_id`** | 素材 CRUD API；`jobs` 任务表与轮询；文件存储 + **hash 唯一索引（成本控制 C2）** | 素材工作台页：上传、素材清单表、进度轮询 |
| **D3**<br>9/18 | 图片/扫描页 **PaddleOCR 本地解析** + 人工校对；PPTX 解析；存疑处标注 | 块表写入与切块；FTS5 索引；`/blocks`、`/markdown`、`/outline` | Markdown 预览（页码锚点跳转）；素材清单 UI；失败重试交互 |
| **D4**<br>9/19 | Stage 1 收口：失败隔离、重试、质量自评；**跑 A1-*** | **评测集 E-1 金标准录入**（组织）；E-2 字段核对样本 | 质量报告页；**E-3 答疑问题集编写** |
| **D5**<br>9/20 | Dockerfile 单容器；**部署上线（M3 硬里程碑）**；持续部署脚本 | P3 契约（章节目录识别）实现与调优；`/extract/knowledge` + jobs | 前端构建接入后端静态托管；生产环境验证 |
| **D6**<br>9/21 | **P4 抽取在 LearnBuddy 上跑通**（Prompt 对齐契约）+ 结果导出/入库脚本；本地 3B 兜底通道 | 逐节抽取流水线；归一与去重；embedding 生成与入库；schema 校验 | 知识图谱页骨架（Cytoscape.js）；知识点详情抽屉；**人工校验工作台 UI** |
| **D7**<br>9/22 | **★结构线索生成 + DAG 拓扑校验与剪枝（12h）** | **★P10 依赖边语义判定 + 融合与冲突标记（核心）** | **★图谱可视化（难度色阶 + 前置边）+ 学习路径页** |
| **D8**<br>9/23 | 混合检索层（FTS5 + 向量 + RRF）；越界判定阈值调优 | 苏格拉底状态机后端（**代码层强制降级**）；三件产出（P8） | 答疑页：SSE 流式 + 溯源卡片 + 诊断面板 + 三轮进度可视化 |
| **D9**<br>9/24 | 全量跑 **A1-* / B1-2**；修本链路缺陷 | 全量跑 **A2-* / B1-1 / B1-5**；出跑测脚本与报告 | 全量跑 A3-* / A4-2 / B1-3；**构造 B1-4 对抗性测试输入** |
| **D10**<br>9/25 | README + 架构文档 + `.env.example` + 仓库清洁 | 评测报告归档；**预置示例材料（A4-7）**导入与验证 | Demo 视频脚本与录制；PPT；社媒连载；LearnBuddy 记录整理 |

> **D7 是创新点的集中攻坚日，三人全部投入同一件事** —— 这一天不做任何与依赖图无关的工作。理由：它是权重最大的「技术创新性 30%」的主要举证，且 D7 之后没有第二天的余量。

### 9.6 共同工作（硬性分配，避免"没人负责"）

| 工作 | 说明 | 分配 |
|---|---|---|
| 评测集 E-1 / E-2 / E-3 | ≥60 个金标准知识点 + 20 题字段核对 + 30 个材料内问题 + 20 个越界问题（§2.6） | **P2 主责组织，三人各出三分之一** —— 谁都不能只当被评测者 |
| LearnBuddy 每日归档 | 当天关键对话整理进 `docs/buddy-logs/`（§5.5） | 按日轮值（P1 → P2 → P3 循环） |
| 常见误区校验 | `kp_misconceptions` 的 `cause` / `remedy` / `trigger_pattern` 必须符合真实学生犯错模式 | 三人交叉互审；标 `source=human` 前需三人过目 |
| 每日站会 | 昨天完成 / 今天计划 / 阻塞项（15 分钟） | 全员，指定 1 人主持并记录阻塞项 |

> **丢了 +2 跨专业加分之后，分工的清晰度就成了 PPT 里必须讲明白的事**：三个同专业的人凭什么做得完跨前端后端、跨解析与交互的完整作品 —— 靠的是清晰的角色纵切与接口约定（本 SPEC 与 `docs/api-spec.md` 就是约定的书面化）。这本身就是在回答"工程完整性 25%"。

---

## 10. 风险清单与降级预案

| 编号 | 风险 | 概率 | 影响 | 缓解 / 降级预案 | 触发条件 |
|---|---|---|---|---|---|
| R-01 | **10 天内做不完**（**含已发生的 1 天排期偏移**，§8 注） | **中高 → 高** | 致命 | ① SQLite 免运维；② 不做鉴权；③ 单容器部署；④ D9 功能冻结；⑤ **D4 EOD 复核一次进度，统一决定从哪里补这一天**（优先砍 §5.4 的 A2 / A3，创新点 28 人时不动）；⑥ 降级：若 D7 仍未跑通 Stage 2，砍掉知识图谱可视化只保留树形列表 | **D4 EOD 复核**；D7 EOD 未达 M4 |
| R-02 | LLM 结构化输出不稳定（JSON 格式错误、字段缺失） | 高 | 中 | Schema 校验 + 最多 2 次重试 + 失败显式落库；Prompt 契约里给 few-shot 示例 | 校验失败率 > 20% |
| R-03 | 扫描件/手写板书的识别质量差 | 高 | 中 | v1.4 改为 **PaddleOCR + 人工校对**（图片素材仅 3 张）；识别不确定的**显式标为存疑** —— 把局限透明化反而体现工程诚实 | A1-3 低于 85% |
| R-04 | 知识点抽取不一致（同输入两次结果不同） | 中 | 中 | `temperature=0` + 固定 seed；A2-7 可复现性验收项 | A2-7 差异 > 10% |
| R-05 | ~~云服务器购买踩坑或费用超预算~~ | — | — | **v1.7 基本消失**：赛事方明确不用部署到正式环境，无需购买长期主机。若确认需要链接，改用 cloudflared 临时隧道（免费） | 已闭环 |
| R-06 | 时间不够，功能做得半生不熟 | 中高 | 高 | 严格按 §2.4 非目标砍需求；§8 功能冻结线；每日站会暴露进度 | 任一里程碑延期 1 天 |
| R-07 | 评委现场上传材料导致超时/失败，演示翻车 | 中 | 高 | A4-7：内置预置示例材料，一键走完整流程；Demo 视频全程用预置材料 | 视频录制前未就绪 |
| R-08 | **依赖边抽取质量不达标**（B1-1 抽检合理率 < 80%） | 中高 | 高 | ① 先抽节点后抽边，避免边污染节点粒度；② 结构线索兜底；③ **降级预案：退化为"仅结构边"**（按章节顺序生成依赖），学习路径仍可生成且必然无环 | D7 EOD 抽检合理率 < 60% |
| R-09 | 答疑出现幻觉，破坏核心信任 | 中 | 致命 | 检索前置 + 越界双保险 + 溯源卡片强制展示 + A3-3 人工全量审查 | 任何一次检出幻觉 |
| R-10 | 交付材料（视频/PPT）挤压开发时间 | 高 | 中 | D10 一整天专门留给交付物，不再排开发任务；视频脚本 D9 前写好 | — |
| R-11 | **创新点（依赖图）因时间不足未做实** | 中 | 高 | §8.1 已给创新点独占 **30 人时（16%）**；D7 全天三人集中攻坚；**进度落后时砍别处、不砍这里**（§8.2 注） | D7 EOD B1-2 / B1-5 未达标 |
| R-12 | **丢掉 +2 跨专业加分后的补偿不足** | 已发生 | 中 | 加分**已确认放弃、不再投入时间**；把力气压到「技术创新性 30%」（§1.5 主创新点）与「AI 工具使用 25%」；PPT 叙事改为"同专业、三角色分工"（§5.6） | 已发生 |
| R-13 | **100 元预算超支** | 中 | 中 | 先学生认证领券再买主机；开发期一律用经济档模型；按 §6.2 超支应对顺序砍（先砍非必要视觉调用） | 累计支出 > ¥75 且尚未上线 |
| R-14 | ~~视觉模型的免费额度或价格政策变动~~ | — | — | **v1.4 起已消化**：视觉链路改为 PaddleOCR 本地解析，不再依赖任何外部额度 | 已闭环 |
| R-18 | **回复模板化后被评价为"不够智能"** | 中 | 中 | ① 每个 `turn_type` 做 5–6 个变体轮换；② 模板内**必须嵌入检索到的原文片段**（这是"有材料依据"的直接体现）；③ PPT 里把"回复质量由知识点结构化程度决定"作为论点主动讲出来（§4.8） | 试聊 10 轮，明显重复感超过 3 次 |
| R-19 | **作品形态判断错误（v1.7 新增）** | 中 | **高** | 赛事方口径是「提交 demo 即可」，但手册明确要求「可直接访问的在线链接 + 完整源码仓库」——**两者不矛盾，是门槛降低而非要求取消**。若误判为"不用部署、不用交代码"，会同时丢掉必需材料资格与两项评分。**缓解：① 源码仓库与 Demo 视频列为不可省项（§2.3）；② 轻量部署必须做（§4.7）；③ 核心算法必须有代码承载** | 已修正（v1.9） |
| R-20 | **提交的链接在评审期打不开（v1.9 新增）** | 中 | **致命** | ① **D5 起保持常驻部署**，每次改动后重新部署并验证；② 提交前在**另一台设备 + 不同网络**实测（A4-5）；③ **专家智能体页面作为第二入口**（双保险）；④ **主机到期时间必须晚于 10/15 决赛**（别买 1 个月到 9/26 就到期的） | 提交前 24 小时内链接不可访问 |
| R-15 | **赛事不允许调用第三方大模型 API（已发生）** | 已发生 | 高 | **§4.8 完整方案（D-13）**：三层抽取策略 + PaddleOCR 本地视觉 + 运行期零依赖；主创新点不受影响；工时已重排且总额不变 | 已发生 |
| R-16 | **LearnBuddy 抽取效率低或 Credits 不够** | 中 | 高 | ① 抽取 Prompt 严格对齐 P1~P4/P10 契约，减少返工；② 本地 3B 模型作为第二通道；③ 人工校验工作台最后兜底；④ 规模已核算（约 30 次调用），2500 Credits 有余量 | 单节抽取返工超过 3 次 |
| R-17 | **人工校验环节被评委误读为"AI 含量不足"** | 中 | 中 | PPT 明确论证：**AI 干粗活 + 人做裁决是教育场景里更专业的产品形态**；重点展示环路校验、学习路径、根因回溯这套确定性工程（§4.8.5） | — |

---

## 11. 待核实项（阻塞项）

> **规则：以下各项未闭环前，不得据此做实现决策。** 每项需明确负责人与截止时间。

### 11.1 已闭环（v1.1 ~ v1.2 · 由项目负责人确认）

| 编号 | 事项 | 结论 | 影响到的章节 |
|---|---|---|---|
| V-01 | 目标学科与材料范围 | **计算机网络**；做深第 4 章（网络层）与第 5 章（传输层）；材料清单与评测集见 §2.6 | §2.6、§6 D-07、§8 |
| V-02 | 云服务器 / 域名 | **租用云服务器**（轻量档位 2 核 2G）；可选 1 元 cn 域名；**D1 先做学生认证领券再下单** | §4.7、§6.2、R-05 |
| V-03 | LearnBuddy 是否开放可调用 API | **不作为依赖**。产品内全部走自备 API；LearnBuddy 以"对话留痕"举证「AI 工具使用」得分 | §5.5、§6 D-10 |
| V-04 | 音频转写 | **不做**（v1.2 最终判定，推翻 v1.1 的窄通道方案）：术语错识别会沿链路污染溯源原文，直接击穿幻觉率红线；省下的工时投给主创新点（§6.1 决策记录） | §2.3 S1、§4.4、§5.1、§6.1、§6.2 |
| V-05 | 大模型 API 与额度 | ~~视觉走 Qwen-VL，文本走 DeepSeek~~ → **v1.4 变更**：赛事不允许调用第三方 API，改为 **LearnBuddy（主）+ 本地 3B（兜底）+ PaddleOCR（本地视觉）**；Provider 路由表保留但不启用 | §4.2、§4.3、§4.8 |
| V-06 | 团队专业背景 | 3 人**同属计算机与软件学院 · 人工智能专业** → **跨专业组队加分（+2）确定不满足** | §5.6、§9 |
| V-07 | 项目名称 | **析知 XiZhi** | 全文档 |
| V-08 | 三人是否分属不同院系 | **同院系**，跨专业加分口径不成立 → **已放弃，不再投入任何确认动作** | §5.6 |
| V-13 | 三人的真实可用工时 | **每人每天 8 小时 × 3 人 × 10 天 = 240 人时**（可规划 185 人时）→ 排期不需重算，§8.1 已据此细化 | §8.1、§9.1、§9.2 |
| V-14 | **创新点的选择** | 确定为**教学依赖 DAG**（§1.5），调研过程见 `docs/innovation.md` | §1.5、§6 D-12、§7.5 |
| V-16 | **赛事对"唯一指定平台"的口径** | **严格解读成立：产品不得调用非 LearnBuddy 的模型** → 按 §4.8 执行（三层抽取策略 + PaddleOCR + 运行期零依赖）。**不再等待确认，直接执行** | §4.8、§6 D-13 / D-14 |
| V-17 | **中英文教材的术语基准** | 冒烟测试实证英文教材**无独立的 "Congestion Avoidance"** 概念。规则：**E-1 金标准必须以实际使用的那份材料为基准，中英材料分别建基准、不得混用** | §2.6 |
| V-18 | **抽取主通道的可用性** | **已验证通过**（2026-09-17 冒烟：P3/P4/P10 三契约全部通过，9 个知识点 + 10 条边，环数 0）。记录见 `docs/extraction-channel.md` §5 | §4.8.1、§7.5 |
| V-19 | **教材与知识骨架** | **教材定为 *Computer Networks: A Systems Approach*（Peterson & Davie, 6th, CC BY 4.0）**；做深 Ch 3 / Ch 5 / Ch 6，其余章轻量覆盖。§2.6 知识骨架已按教材在线版核对 | §2.6、§6 D-07 |
| **V-22** | **LearnBuddy 平台能力边界（能否挂自建能力）** | **✅ 已验证可用**：平台支持**自定义 Skill**，新建后**立即被识别（无需重启）**。已实际把「知识依赖分析模块」挂成 Skill 并跑通（4 个算法 + 2 组对抗测试全部通过）。完整报告见 [`docs/platform-capability-check.md`](docs/platform-capability-check.md) | §2.3 S8、§4.8.3、§6 D-16、§7.5 |

### 11.2 仍未闭环（本轮新发现，必须闭环）

| 编号 | 待核实事项 | 影响 | 负责人 | 需在何时前闭环 |
|---|---|---|---|---|
| V-09 | 云厂商**学生实名认证与代金券**的实际可用性：认证资格、券的面额、适用范围与有效期 | 中（预算） | P1 | **D1 前** |
| ~~V-10~~ | ~~阿里云百炼新用户免费 token 额度规则~~ —— **v1.4 起不再适用**（视觉链路改 PaddleOCR 本地，模型改走 LearnBuddy） | — | — | 已闭环 |
| V-12 | **§2.6 的知识骨架与材料清单需用手上实际教材/课件核对**（章节号、章节名称可能与我列的通用骨架不一致） | 中（评测集基准） | 全员 | **D2 前** |
| V-15 | **前置依赖边的抽检口径（B1-1）** | **口径与样本已就绪**：三问判定法 + 10 条共识样本（6 真 4 造）+ 分歧处理规则 + 边界案例手册模板，见 [`docs/edge-review-consensus.md`](docs/edge-review-consensus.md)。**待三人各自独立判定并比对分歧 → 闭环后方可开始抽检 50 条** | 中（验收有效性） | 全员 | **D7 前** |
| **V-20** | 初赛是否仍需要"可访问的地址"？评审期间是否必须一直可访问？ | 低 —— **已按保守假设解决**：手册明写是必需材料，**结论是必须部署并提供稳定链接**（§4.7）。本项仅需确认"评审期是否需要一直在线"，以便决定主机购买时长 | 项目负责人 | D1 前（顺带确认，不阻塞） |
| **V-21** | **专家智能体形态的评分口径**：评分表里「工程完整性 25%」对平台配置的智能体如何评价？是否仍需提交完整源码仓库？ | 中 —— 我们已选混合形态，**源码仓库无论如何都要交**（§2.3 硬边界 2），故本项从"决定要不要写代码"降为"确认口径、便于 PPT 表述" | 项目负责人 | D3 前 |

> ⚠️ **V-20 / V-21 的兜底规则**：若到 **D3 仍未确认，则默认按保守假设执行** —— 「仍需要一个可访问链接 + 必须提交源码仓库」。理由是**保守假设不返工**：多做的那部分（源码、算法）本来就是要交的；而按激进假设少做了，事后补不回来。

> v1.5 新增闭环：**V-16**（规则口径 → 严格解读，D1 不再需要确认）、**V-17**（术语基准规则）、**V-18**（抽取主通道冒烟通过）。

> v1.2 移除了三项已闭环条目：**V-08**（同院系，加分不成立）、**V-11**（ASR 选型 —— 音频链路已砍）、**V-13**（工时已确认为 8h/人/天）。

---

## 12. 变更日志

| 版本 | 日期 | 变更内容 | 变更人 | 原因 |
|---|---|---|---|---|
| **v2.1** | 2026-09-17 | **登记排期偏移**：9/16–9/17 的时间用于方案论证与规格撰写，**实际开发从 9/17 开始，整体偏移 1 天**（可用整天 10 → 9）。§8 新增偏移登记说明（不上调目标，记在 R-01 名下，D4 EOD 复核时统一决定从哪补）；新增 `docs/tasks/` 目录与首份逐人任务清单 [`docs/tasks/2026-09-17-P2.md`](docs/tasks/2026-09-17-P2.md) | AI 拟定 | 核实仓库状态后发现的进度差异，主动登记 |
| **v2.0** | 2026-09-17 | **平台能力边界实测验证（V-22 闭环）**：① **实地验证平台支持自定义 Skill 且新建后立即识别（无需重启）** —— 「知识依赖分析模块」已挂成 Skill `xizhi-graph-infer` 并跑通；② **核心算法真正落地**（非伪代码）：`verify` / `prune` / `path` / `gap` 四个算法，用真实抽取数据实测（环数 0、理由完备率 100%、学习路径拓扑有序），并用两组对抗性输入验证剪枝（T1 单一环 / T2 环+自环+缺理由+稀疏性违规）；③ 新增 [`docs/platform-capability-check.md`](docs/platform-capability-check.md) 与 `samples/graph-infer-tests/`、`samples/graph-infer-out/`；④ §4.8.3 补记"图算法层已跑在平台上"与 Skill 挂载路径；⑤ §7.5 **B1-4 夹具部分就绪**；⑥ §9.1 P1 目录新增 `pipeline/graph_infer.py`；⑦ 实测发现并修正一个真 bug（`depended_by_count` 用错度数方向），并新增"孤立知识点必须显式列出"检查（真实数据检出 2 个孤立节点，其中 1 个是真实漏抽） | AI 实测 + 拟定 | 用户要求"自己建一个脚本尝试一下"，用实测替代猜测 |
| **v1.9** | 2026-09-17 | **修正 v1.8 的部署决策（重要）**：当把手册 §二「作品须部署至浏览器环境，提供**可直接访问的在线链接**」与赛事方口头的「不用部署到正式环境」放在一起看时，正确解读是**门槛降低而非要求取消**。① §1.1 / §2.3 S8 更新部署口径，新增**兼容落点说明**并明确排除"临时隧道糊弄"；② **§4.7 恢复「轻量部署必须做」**，D5 硬要求恢复（但不做正式环境）；③ **§6.2 预算回补主机费用**，改为 ¥0 ~ ¥79（学生券后可能仍为 ¥0~10）；④ **§8.1 工时回补部署 3→6**，从专家智能体（7→5）与交付物（5→4）匀出，总额仍 185；⑤ §8.2 D5 与排期纪律恢复"部署不推迟"；⑥ D-16 定稿并新增第 4 条硬边界（链接必须常驻可访问）；⑦ R-19 更新为"已修正"，新增 **R-20（链接在评审期打不开）**；⑧ V-20 降为"顺带确认" | 项目负责人引手册原文 + AI 修正 | **手册书面要求优先于口头口径** —— 评审按手册走 |
| v1.8 | 2026-09-17 | **作品形态决定**：① **D-16：混合形态**（LearnBuddy 专家智能体 + 自建 Web Demo）；② ~~决定不买主机、不做正式部署~~（**已被 v1.9 修正**）；③ §1.1 / §2.3 S8；④ §4.7 主机与公网入口调整；⑤ §8.1 工时重排：新增「LearnBuddy 专家智能体」，素材解析 44→42、校验工作台 10→8，总额仍 185；⑥ §8.2 D8/D9 插入专家智能体配置与上线；⑦ V-20 / V-21 降级为"确认口径" | 项目负责人确认形态 + AI 拟定 | 赛事方口径变更 + 用户拍板 |
| v1.7 | 2026-09-17 | **赛事方口径变更：初赛提交 demo 即可、不用部署到正式环境；LearnBuddy 智能体可为 web demo 或专家智能体。** ① 新增 [`docs/delivery-form.md`](docs/delivery-form.md)：三形态方案对比、**混合形态推荐**、两者分工设计、成本工时风险变化、四个待确认问题清单；② §1.1 / §2.3 更新作品形态定义，新增 **S8 作品形态**与三条硬边界；③ **§4.7 正式部署降级为备选**；④ §8.2 D5 里程碑改写；⑤ §10 **R-05 闭环**、新增 **R-19（形态判断错误）**；⑥ 新增决策 **D-16**、待核实项 **V-20 ~ V-22**（含保守假设兜底规则） | 项目负责人转述赛事方口径 + AI 拟定 | 赛事方明确了作品形态口径 |
| v1.6 | 2026-09-17 | ① **教材确定**（V-19）：Peterson & Davie 6th（CC BY 4.0），§2.6 知识骨架按其章节体系重写、做深 Ch 3 / Ch 5 / Ch 6，材料清单 M-A ~ M-H 重写（新增中文材料 M-H 与英文教材的答辩说法）；② **新增 `docs/edge-review-consensus.md`**（V-15 口径材料）：三问判定法、10 条共识样本（6 真 4 造）、分歧处理规则、边界案例手册模板、完成标准；③ **§9 团队分工大幅细化**：新增文件级目录归属与归属纪律（9.1）、接口约定与冻结纪律（9.2）、协作纪律（9.3）、**抽取操作的执行分配与交叉校验（9.4）**；逐日任务表更新教材切分与材料自造任务 | 项目负责人确认 + AI 拟定 | 用户确定教材、要求先做判定共识、要求细化分工 |
| v1.5 | 2026-09-17 | ① **规则口径确认（严格解读）**，V-16 闭环，§4.8 方案转为正式执行基线；② **创新点表述定稿（D-15）**：改为「AI 预抽取 + 人工校验的依赖图构建，并首次把『教学依赖图必须无环』作为工程不变量强制保证」，§1.5 补答辩口径；③ **抽取主通道冒烟验证通过**（V-18）：新增 `docs/extraction-channel.md`（通道规格 + 操作规程 + 6 项冒烟发现）与 `samples/channel-smoke-test/`（P3/P4/P10 三份可复用的测试夹具）；④ **新增 `docs/materials-and-licenses.md`**：开放材料清单与许可核实、材料清单重写（含版权合规来源）、中文材料三条路径、术语基准规则；⑤ §2.6 新增**术语基准规则**（V-17）；⑥ `prompt-contracts.md` 修正 P4 移除 `prerequisites`、P10 新增 `deferred_edges` 并修正跨章候选对裁剪 | 项目负责人确认 + AI 执行 | 用户确认规则口径 + 要求打通抽取通道 |
| v1.4 | 2026-09-17 | **"赛事不允许调用第三方 API"约束已确认**：① §4.8 从"预案"改为"执行方案"，新增三层抽取策略（LearnBuddy 主 / 本地 3B 兜底 / 人工校验）、PaddleOCR 视觉链路、工时重排（§4.8.4，总额仍 185 人时）、Marble 数据校验举证（§4.8.6）、规则口径核实（§4.8.7）；② §4.2 技术栈改为「LearnBuddy + 本地 3B + PaddleOCR + 运行期零 LLM」；③ §4.3 路由表标注为"保留不启用"；④ §4.4 视觉路径改为 PaddleOCR 为主；⑤ §8.1 新增「人工校验工作台」10 人时并重算各模块；⑥ 决策新增 D-14 / D-15，D-09 暂停、D-13 定稿；⑦ 风险升级 R-15、新增 R-16 ~ R-18、修订 R-03；⑧ 新增待核实项 V-16 ~ V-18 | 项目负责人确认约束 + AI 拟定 | 赛事规则限制与开发机算力限制 |
| v1.3 | 2026-09-17 | 新增 **§4.8 LLM 依赖降级预案**（假设完全无法调用第三方 API 时的完整应对：离线构建 + 在线确定性推理、回复模板化设计、三条候选路线对比、额外收益论证）；新增决策 **D-13**、风险 **R-15** | AI 拟定 | 用户提出的风险假设 |
| v1.2 | 2026-09-17 | ① **音频转写从"窄通道"改为"完全不做"**（D-08 反转，新增 §6.1 决策记录与答辩话术），同步清理 §2.3 / §4.3 / §4.4 / §5.1 / §6.2 / §8 / §10 / §11 与 `prompt-contracts.md` 的 P9；② **跨专业加分确认放弃**（三人同院系同专业），§5.6 重写补偿策略；③ **新增 §1.5 创新点与差异化定位 + `docs/innovation.md`**（竞品与文献调研、空白定位、双通道融合改进方案），新增决策 D-12；④ **新增 §7.5 创新点专项验收 B1-1 ~ B1-6**，新增 F2.4 重写 / F2.10 / F2.11 / F3.8，新增 P10 契约；⑤ **工时确认 3 人 × 8h × 10 天 = 240 人时（可规划 185）**，新增 §8.1 工时预算与模块分配、**§9.2 逐日逐人任务表**；⑥ 风险表 R-08 / R-11 改为依赖图相关风险；⑦ 待核实项 V-08 / V-11 / V-13 闭环，新增 V-14 / V-15 | 项目负责人确认 + AI 拟定 | 讨论阻塞项与创新点后按确认结果同步 |
| **v1.1** | 2026-09-16 | ① 项目定名 **析知 XiZhi**；② 目标学科锁定 **计算机网络**，新增 §2.6（知识骨架、材料清单 M-A~M-F、评测集 E-1~E-3）；③ 部署方案具体化（轻量云服务器 + 成本），新增 §6.2 成本预算与 7 条成本控制措施；④ 模型分工明确为 **Qwen-VL 视觉 + DeepSeek 文本**，§4.3 新增 Provider 路由表；⑤ 音频转写决策为**窄通道**，新增 §6.1 利弊分析；⑥ 新增决策 D-07 ~ D-11；⑦ **跨专业加分判定为不满足**，§5.6 新增丢分补偿策略，§9 分工重写；⑧ 新增风险 R-11 ~ R-14；⑨ 待核实项 V-01 ~ V-07 闭环，新增 V-08 ~ V-13 | 项目负责人确认 + AI 拟定 | 讨论阻塞项后按确认结果同步 |
| v1.0-draft | 2026-09-16 | 建立初版规格：架构形态、模型来源、范围优先级三项决策已由负责人确认（D-01 ~ D-03） | — | 项目启动 |

**变更流程**：任何范围/技术/交互的调整 → 先在此表登记 → 修改对应章节 → 升版本号 → 同步给全组 → 再改代码。

---

## 13. 附录

| 附件 | 路径 | 说明 |
|---|---|---|
| 数据模型详细定义 | [`docs/data-model.md`](docs/data-model.md) | 全部表、字段、类型、约束、枚举 |
| 接口规格 | [`docs/api-spec.md`](docs/api-spec.md) | REST 端点、请求/响应结构、错误码、SSE 约定 |
| Prompt 契约 | [`docs/prompt-contracts.md`](docs/prompt-contracts.md) | 每个 LLM 调用的输入输出契约与 JSON Schema |
| **逐人每日任务清单** | [`docs/tasks/`](docs/tasks/) | 把 §9.5 的逐日表拆成"某人某天具体做什么"的可执行清单。首份：[`2026-09-17-P2.md`](docs/tasks/2026-09-17-P2.md) |
| **平台能力边界验证报告** | [`docs/platform-capability-check.md`](docs/platform-capability-check.md) | **v2.0 新增**：三种挂载形态可行性、核心算法实测记录、6 项发现、D1 落地建议 |
| **图推理算法测试夹具** | [`samples/graph-infer-tests/`](samples/graph-infer-tests/) | 两组对抗性输入（单一环 / 环+自环+缺理由+稀疏性违规），可直接固化为 pytest 用例 |
| **作品形态决策分析** | [`docs/delivery-form.md`](docs/delivery-form.md) | 赛事方口径变更、三形态方案对比、混合形态推荐与分工、成本工时风险变化 |
| **创新点调研与改进方案** | [`docs/innovation.md`](docs/innovation.md) | 同类项目盘点、学术文献、空白定位、双通道融合设计、B1-* 指标依据 |
| **前置依赖边判定口径与共识材料** | [`docs/edge-review-consensus.md`](docs/edge-review-consensus.md) | **V-15 的执行材料**：三问判定法、10 条共识样本、分歧处理规则、边界案例手册 |
| **抽取通道规格与操作规程** | [`docs/extraction-channel.md`](docs/extraction-channel.md) | 三条通道的分工、主通道 6 步操作、入库校验、**冒烟测试记录与 6 项发现** |
| **材料来源与许可合规** | [`docs/materials-and-licenses.md`](docs/materials-and-licenses.md) | 开放材料清单与许可核实、M-A~M-H 来源建议、中文材料三条路径、合规动作清单 |
| **抽取通道测试夹具** | [`samples/channel-smoke-test/`](samples/channel-smoke-test/) | P3/P4/P10 三份真实产出，可直接用于 schema 校验、入库测试、图算法测试、前端联调 |
| 赛事手册 | `粤港澳大湾区AI Coding 创新大赛赛事手册(4).docx` | 原始约束来源 |
| LearnBuddy 使用记录 | `docs/buddy-logs/` | 每日开发对话归档（待建）。**注：`docs/extraction-channel.md` §5 的冒烟测试本身就是第一份可用记录** |
| **版本配套** | **SPEC v2.1 ↔ prompt-contracts v1.3 ↔ data-model v1.2 ↔ api-spec v1.2 ↔ innovation v1.0 ↔ extraction-channel v1.0 ↔ materials-and-licenses v1.0 ↔ edge-review-consensus v1.0 ↔ delivery-form v1.1 ↔ platform-capability-check v1.0** | 文档版本必须**同步升版**，不允许单份漂移。发现不一致时，以 SPEC 为准并立即修正其余各份 |

---

### 提交前自检清单（9/26 用）

| ✓ | 项 |
|---|---|
| ☐ | 五份交付物齐备：作品链接 / 源码仓库 / 3 分钟 Demo 视频 / 介绍 PPT / LearnBuddy 使用记录 |
| ☐ | **Web Demo 稳定链接 + 专家智能体页面链接，两个都提交**（双保险，D-16 硬边界 4） |
| ☐ | 链接在**另一台设备 + 不同网络**实测可打开（A4-5）；**主机到期时间必须晚于 10/15 决赛**（R-20） |
| ☐ | 专家智能体与 Web Demo 入口互指通畅，两处答疑表述一致（D-16） |
| ☐ | **源码仓库一个都不能省** —— 「工程完整性 25%」与「技术创新性 30%」的唯二证据（§2.3 硬边界 2） |
| ☐ | 仓库有 README + LICENSE + `.env.example`，无任何真实 Key 泄漏 |
| ☐ | Demo 视频用**预置示例材料**走完整流程（A4-7），不依赖现场上传 |
| ☐ | **Demo 视频里 §1.5 主创新点（依赖图 → 学习路径 → 卡点回溯）被清晰展示** |
| ☐ | PPT 包含：作品简介、赛题方向、AI 能力说明、技术方案、团队成员信息（手册要求五项） |
| ☐ | PPT 有**「作品范围与取舍」一页**，如实写明音频转写的放弃理由（§6.1 答辩话术） |
| ☐ | PPT 中**没有**任何暗示"跨专业组队"的表述（§5.6） |
| ☐ | 验收指标 A1-* ~ A4-* **与 B1-* 创新点专项**均已全量跑过并留有记录 |
| ☐ | 音频相关 UI 入口、文案、代码路径均已干净移除（不留半成品痕迹） |
| ☐ | **合规自查**：产品代码内不含任何对第三方大模型 API 的调用（全局搜索外部域名与 API Key，只应有 LearnBuddy 的**开发期**使用记录） |
| ☐ | PPT 中如实说明"抽取为 AI 预抽取 + 人工校验""图片解析为本地 OCR + 人工校对"（§4.8） |
| ☐ | 完成提交（**上午提交，不拖到 23:59**） |

---

*本文档为项目唯一权威源。规格先行，代码随后；偏离即缺陷。*
