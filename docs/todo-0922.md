# 待办清单 · 9/22（D+6，距 9/26 截止 **4 天**）

> 生成时间：2026-09-22 09:45 · 生成人：P2
> 依据：远端 `origin/main`（`966ae14`）+ 未合并分支 2 个 + 打开 Issue 15 条 + 各 Issue 评论
>
> **口径**：只列**需要某个人动手**的事。已完成的不列，纯讨论的不列。

---

## 一、先看阻塞关系（**按这个排，不按紧急感排**）

```
① hard 边缺失 ──→ 卡住 3 件事：录制纪律④ / B1-3 验收 / /path 页演示
                  ↑ 这是当前**唯一的链式阻塞**
                    （P3 的 #94 + P2 的 #103 是同一件事）

② E-3 全量跑 ──→ 卡住 A3-1（≥85%）/ A3-2（≥90%）两个验收项
                  ↑ 需要 ① 先定（否则跑出来的数还要重跑）

③ 仓库转 public ──→ 交付物「源码仓库」**这项可能直接作废**（不是扣分，是作废）
                     ↑ 和 ①② 无依赖，**可以并行，而且越早越好**

④ 演示脚本 / 视频 / PPT / 对话记录 ──→ 依赖 ①②③ 的结论
```

---

## 二、P2（我）· 待办

| # | 待办 | 卡着谁 | 我的判断 |
|---|---|---|---|
| **P2-1** | **产出一批 hard 边**（20–40 条，含 `reason` + `evidence_quote`）→ 过 P3 目 → 灌 `POST /api/review/import-edges` | **P3 录制 / B1-3 验收** | **最优先** —— 它是唯一链式阻塞的源头。端点已实现、测试已覆盖 |
| **P2-2** | 把 `#103` 与 `#94` **合并成一条**（同一问题，两条会分散讨论） | 团队 | 10 分钟 |
| **P2-3** | 失败样本判据：把「**命中 1 个点 vs 命中一整节**」写清楚（**不基于 6 个样本改阈值**） | A3-2 | D9 全量跑之前定判据，否则是"对着数据调参数" |
| **P2-4** | `conflict_count` 改名建议（`unreviewed_edge_count`）—— 已在 `#103` 提，**等 P3 口径** | 契约 | **排在 P2-1 之后** —— 改名要走流程，别挤掉更该修的 |
| **P2-5** | **仓库转 public 的执行**（`#75`）—— 我给日期建议，**美佳点确认** | **交付物作废级** | ⚠️ **这条其实最急**，但只有美佳能拍板 |

---

## 三、P3 · 待办

| # | 待办 | 状态 |
|---|---|---|
| **P3-1** | **review `#89`**（SPEC 差异登记 D-01） | 他自己在 `#94` 里说「待 review」 |
| **P3-2** | **`#89` 定案后改 SPEC**（冻结表 + D6 任务行 + §12 变更日志）| 我已给具体改法与依据，**等他确认措辞** |
| **P3-3** | 演示脚本按 **A/B 两套演法**定稿（`#97` 已补） | 未合并 |
| **P3-4** | **录制**（`deliverable-plan.md` 分工里 Demo 视频是他的） | ⚠️ **被 P2-1 阻塞** |
| **P3-5** | D9 全量跑 30+20 题时，**每题落 `kp_ids` + `is_out_of_scope` 表格** | 他自己说的，等 P2-3 的判据 |
| **P3-6** | **合并 `#96` / `#97` / `#98` 三个 PR**（E-3 / 演示脚本 / B1-4 对抗输入） | 都已推上去，待 review 合并 |

---

## 四、P1 · 待办

| # | 待办 | 状态 |
|---|---|---|
| **P1-1** | **合并 `fix/setup-venv-base-deps-check`** —— **这条改动完全正确**，我已复核 | ⭐ **立刻可合** |
| **P1-2** | **裁决 `#103` 的 hard 边修法**（方案 A 自动判 / 方案 B 人工确认） | 我在 `#94` 里给了两个方案 + 倾向 B + 理由 |
| **P1-3** | **`_HARD_HINTS` 是启发式的，能不能用来判"硬前置"** —— 我在 `#103` 问过，**没回** | 这是 P2-1 的前提（若走方案 A 就要他实现） |
| **P1-4** | `#55` 素材请求（已给 3 份节选）——**可关吗？** | 待确认 |

---

## 五、美佳（只有你能做）

| # | 待办 | 为什么只有你 |
|---|---|---|
| **M-1** | 🔴 **仓库转 public —— 定个日期并执行** | 交付物「源码仓库」**这项可能直接作废**。我给不了日期，也按不了这个键 |
| **M-2** | **LearnBuddy 对话记录导出** | 只有你的账号能导（`deliverable-plan.md` §二） |
| **M-3** | **代表提交**（手册：1 人代表） | 同上 |
| **M-4** | 拍板 `P2-1` 的 hard 边**是否走人工确认**（方案 B） | 这是"改数据"还是"改代码"的分叉，影响 D9 验收口径 |

---

## 六、⭐ 我今天建议的顺序（**给一句理由**）

| 序 | 做什么 | 一句话理由 |
|---|---|---|
| **1** | **仓库转 public**（M-1） | **它是唯一"不做就作废"的**，其他都只是扣分 |
| **2** | **合并 P1 的 setup-venv 分支**（P1-1） | 已复核通过，10 秒的事，且它防的是"测试跑不起来" |
| **3** | **产 hard 边候选清单**（P2-1） | 唯一链式阻塞的源头，解开它同时放行 3 件事 |
| **4** | **合并 P3 的三个 PR**（P3-6） | 它们已推上去，不合并就是"躺在分支上的成果" |

**⚠️ 我要说一句不中听的**：`M-1`（仓库转 public）**在清单上排第 1，但它是唯一一件"没人提"的事**。
`#75` 开了两天，没结论。**如果 9/26 23:59 那一刻仓库还是私有，评委打不开链接 —— 这一项直接作废，不是扣分。**

---

## 七、我**不建议**现在做的（避免分散）

| 事 | 为什么不做 |
|---|---|
| 改 `conflict_count` 字段名 | 只是"名字让人多想一步"，**严重性差一个量级**，别挤掉 hard 边 |
| 基于 6 个失败样本调越界阈值 | **n=6 调参数就是对着数据拟合**，等 D9 全量 |
| 补更多截图 / 素材 | 已齐（6 静态 + 6 交互 + 2 架构图 + 数字快照），**够了** |
