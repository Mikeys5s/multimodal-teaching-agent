# 待办清单 · 9/23（D+7，距 9/26 23:59 **3 天 15 小时**）

> 生成：2026-09-23 08:55 · 依据：`origin/main`（`be05ce8`）+ 已核实的三项交付物状态
>
> **口径**：只列**还没完成**的事。已完成的不列 —— 那些在 `docs/todo-0922.md` 里有。

---

## 一、⭐ 一句话现状

**技术侧全绿，交付物五项里三项已完成。剩下的三件全部不在我手上（或需要你拍板）。**

| 交付物 | 状态 | 谁 |
|---|---|---|
| ① 在线链接 | ✅ **HTTP 200 / 128ms** | 已达成 |
| ② 源码仓库 | ✅ **public**（**提前达成**，原计划 9/25） | 已达成 |
| ③ Demo 视频 | ⏳ **未产出** | **P3** |
| ④ 作品 PPT | ⏳ **v2 已出 692KB** | **等美佳验收** |
| ⑤ 对话记录 | ⏳ **未导出** | **美佳** |

---

## 二、🔴 只有你能做（3 件）

| # | 待办 | 为什么只有你 | 建议时机 |
|---|---|---|---|
| **M-1** | **打开 PPT v2 验收** —— 告诉我哪页要改 | 我是作者，看不了自己的排版（本机没有 LibreOffice，`slidep screenshot` 也走那个坏服务） | **今天最早一件事** |
| **M-2** | **导出 LearnBuddy 历史对话记录** | 赛事要的是**平台上的历史对话**，只有你的账号能导 | **今天**（它可能比想象中麻烦） |
| **M-3** | **回我一条：团队三人真实姓名 + 分工** | 我只知道 GitHub id（`DakerDack` / `Mikeys5s` / `RyeYen`） | 随时，我补第 14 页 |

**⚠️ M-2 我标了红** —— 因为它的**不可控性最高**：导出可能要审核、可能格式要转、可能要挑会话。
**而它是五项里唯一"完全没开始"的一项。**

---

## 三、可以立刻做的（我，不需要等）

| # | 待办 | 卡着谁 | 备注 |
|---|---|---|---|
| **P2-1** | **合并 `#110`**（P3 的 `fix/path`：hard=0 时不再断言「它是起点」） | —— | **唯一未合并的 PR**，且它修的正是我们昨天踩过的坑（把"不知道"当结论） |
| **P2-2** | **关掉 4 条已完成的 Issue**：`#103`（hard 边已灌）/ `#75`（已 public）/ `#55`（素材已给）/ `#52`（排期已落地） | —— | **开着会让"待办"看起来比实际多** |
| **P2-3** | **把 15 秒「AI 判反、人判正」写进 `demo-script.md`** | **P3 录制** | P3 说过这是最好的答辩素材，**我该把它落成脚本** |
| **P2-4** | 若你验收后 PPT 要改 → 我改（改完是 v3） | 等 M-1 | —— |

---

## 四、P3 那边（他昨天 07:12 说过在等）

| # | 待办 | 依赖 |
|---|---|---|
| **P3-1** | **演示脚本定稿到 A 方案**（hard 边已灌，`hard_edge_count = 15`） | ✅ **已解锁** —— 我昨天告诉他了 |
| **P3-2** | **录视频** | 脚本定稿 |
| **P3-3** | `#89`（SPEC 差异 D-01）定案 | 他说"待 review" |
| **P3-4** | 回我 `#94`（我昨天报了灌库结果 + 三层判据） | —— |

**⚠️ 我昨天在 `#94` 里提醒过他一句**：**录制取景要避开重复段**（图上有 4 份重复内容，
近景镜头会拍到同一个标题连排 4 次）。**这条他还没回。**

---

## 五、P1 那边

| # | 待办 | 状态 |
|---|---|---|
| **P1-1** | `#112`（629 重复）**给个处置意见** | **我昨天开的，0 评论** |
| **P1-2** | `#94` 里「`section_key` 为什么带批次号」 | 我猜是"每次抽取生成一个前缀"，**要他确认** |

**⭐ 这两条都不阻塞交付** —— 我昨天已把口径改掉（`ppt-material.md`），
**所以即使 9/24 之后才去重，答辩口径也不会出错。**

---

## 六、⭐ 我建议的今天顺序（**给一句理由**）

| 序 | 做什么 | 一句话理由 |
|---|---|---|
| **1** | **M-2 导对话记录** | **它是唯一"完全没开始"且不可控的**，其余两件（视频/PPT）都已经有东西在手 |
| **2** | **M-1 验收 PPT** | 有修改意见我才能改，**改完还要留时间给你复审** |
| **3** | **P2-1 / P2-2**（合并 + 关 Issue） | 10 分钟的事，**但能让"剩余待办"从 20 条变成 5 条** |

---

## 七、我**不建议**现在做的

| 事 | 为什么不做 |
|---|---|
| **给 PPT 加更多页** | 14 页够讲 3 分钟；**加页是加工作量，不是加信息量** |
| **改 629 重复的数据** | 已定：**排到 9/24 功能冻结之后**（P3 的判断，我同意） |
| **补更多截图** | 15 张静态 + 交互态够了 |
| **为"看 PPT 效果"装 LibreOffice** | 那是环境成本，不是交付物 —— **你打开看一眼比它快** |
| **再深挖 `#20`（文件被删）的定责** | 20 条评论了，**它不阻塞任何交付** |
