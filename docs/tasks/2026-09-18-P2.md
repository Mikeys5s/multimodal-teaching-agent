# P2 任务清单 · 2026-09-18（D2）

> 基线：`64a0eef`（本地 = 远端）· 217 passed / 28 端点 / 14 张表
> 上游：[`../../SPEC.md`](../../SPEC.md) §8 排期 · §9.2 接口约定
> 上一阶段：[`2026-09-17-P2-step3.md`](2026-09-17-P2-step3.md)（步骤 3 已完成）

## 0. 昨天的收尾状态

| 项 | 状态 |
|---|---|
| 接口冻结（I-3） | ✅ 28 端点 + 7 个契约文件，SPEC §9.2 已标记 |
| 数据模型 | ✅ 14 张表，迁移可逆 |
| 测试 | ✅ 160 → **217 passed**（契约守卫 + 冒烟测试） |
| 冻结通告 | ✅ Issue #5 |
| venv 反复被删 | ✅ **已绕过**（项目外 venv + junction），**活过一夜** |
| 删文件元凶 | ❌ **仍未定责**（见 R1） |

---

## 1. 🔴 今天优先

### T1 · 把契约差异同步给 P3（最高优先）

**为什么最急**：P3 的 `frontend/src/lib/types.ts`（489 行）注释里写着
「严格对齐 `docs/api-spec.md` **v1.2**」，而我们昨天冻结的是 **v1.3**。
**他们越往后写，返工越贵。**

v1.2 → v1.3 的 6 处差异里，**对 P3 有实际影响的有 4 处**：

| # | 差异 | 对前端的影响 |
|---|---|---|
| 1 | **非 JSON 响应**（markdown / CSV 下载）的 `request_id` 走 `X-Request-ID` 响应头；**出错时仍返回 JSON 包封** | 下载类请求要**先看 Content-Type** 再决定解析分支 |
| 2 | **SSE 事件带 `id: <seq>`**，且 `id:` 行必须在 `event:` 之前 | P3 还没写 SSE（`Tutor.tsx` 17 行是占位）。**现在告知成本最低** |
| 3 | `needs_review` **严格布尔**，传 `1`/`0` 会 **400**（不是静默兼容） | 他们已有该字段，但要知道不能传 `1` |
| 4 | `student_evidence` 是**可重复**查询参数 | 卡点回溯页要用；无效值**忽略不报错** |
| 5 | 分页默认 `page=1` / `page_size=20` / 上限 100，越界 400 | 前端分页控件照此写死 |
| 6 | 新增 `GET /api/health/pragma`（第 28 个端点） | 演示自检页可用 |

- [ ] 发 Issue 列出上表，@P3
- [ ] **顺带提醒：同步 main 用 `merge`，不要 `rebase`**（见 R2）
- [ ] 审查 P3 的 `types.ts` / `api.ts` / `endpoints.ts` 有无其它偏差

**已知的 P3 缺口**（审查初步结论）：

- `student_evidence`、`X-Request-ID`、`answer_missing`、`source_quote` **均未定义**
- `seq` 只出现 1 处 → SSE 的 `id:` 续推机制基本没覆盖（属正常，未开工）

### T2 · 部署方案落地（M3 = 9/20，硬里程碑，不推迟）

只剩 **2 天**，且这一步归 P2。

- [ ] **确认云主机是否已采购**（依赖 V-09：学生实名认证 → 领代金券 → 下单，顺序不能反）
- [ ] 定下**单容器**方案：FastAPI 同时托管前端产物（`/` 静态 + `/api` 接口，同源无 CORS）
- [ ] `frontend` 构建产物接入后端的路径约定（写进 SPEC 或 README）
- [ ] 出一份可复现的部署脚本 / 步骤

---

## 2. 本周内（M3 / M4）

### T3 · `scripts/evaluate.py` —— 质量报告的数据源

质量报告页（端点 19）现在返回的是 mock。这个脚本是让它变成真的那条路：
- [ ] 三级结构完整率（A2-1）
- [ ] 溯源覆盖率（A2-3）
- [ ] 前置依赖抽检（B1-5 的 `prerequisite_sampling_pass_rate`）
- [ ] 幻觉率 / 拒答率统计

### T4 · I-1 接收端准备（P1 → P2，D3 EOD 到）

- [ ] 明确 `material_blocks` 的入库约定（块锚点、`section` 归属）
- [ ] 写数据校验：缺 `section_id` / 缺 `source_quote` 的块要能报出来
- [ ] `scripts/ingest.py` 的对接点（P1 提供 CLI，我这边准备表与约束）

### T5 · `MOCK_MODE` → 真实实现（依赖 T4）

各路由文件里的 `MOCK_MODE` 常量置 `False` 并接上真实查询。
**接口签名与响应结构不动** —— 这正是当初把它做成一个开关的原因。

### T6 · V-12 知识骨架用实际教材核对（D2，阻塞评测集基准）

- [ ] 用手上的计算机网络教材 / 课件核对 SPEC §2.6 的知识骨架
- [ ] 核对结果直接决定评测集怎么建

---

## 3. 风险与待定

| 编号 | 事项 | 状态 |
|---|---|---|
| **R1** | **「谁在删文件」仍未定责** | 🔴 P1 提供了关键线索：`git rebase` 会删掉 `.git/refs/`，他们**复现了两次**（先备份后复现，可排除偶发）。但这条**解释不了** `.venv/site-packages/` 被清空那 4 次（那与 git 无关）。**待做**：查火绒隔离区 + 日志；开 `watch_files.py` 抓现行 |
| **R2** | `git rebase` 会破坏仓库 | 🔴 P1 已确认。**纪律**：同步 main 一律用 `merge` 或新克隆。P1 的坑 E 需并入 `dev-environment.md`（P1 说属共享文档，先在 Issue 知会） |
| **R3** | 仓库仍是私有 | 🟡 交付前必须转 public，否则评委打不开＝该项作废。可以早点做，免得忘记 |
| **R4** | 云主机是否已采购 | 🟡 未知。**若未买，M3（9/20）会滑** —— 采购 + 学生认证有前置顺序 |
| **R5** | 删文件问题对 P3 的影响未确认 | 🟡 P3 在另一台机器上；需确认他们的环境是否也有此现象 |

---

## 4. 今天建议的执行顺序

1. **T1**（契约差异同步）—— 阻塞 P3，且拖一天贵一天
2. **T2 的第一项**（确认云主机）—— 一句话的事，但决定 M3 会不会滑
3. **T3**（`evaluate.py`）—— 不依赖任何人，可以立刻开工
4. T6 / 其余按时间余量推进

> **纪律提醒**：每完成一小块就提交推送。本机有未定责的删文件问题，
> **攒改动最后一起推是风险的**。
