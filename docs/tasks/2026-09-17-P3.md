# P3 今日任务清单 · 2026-09-17（实际 D1 + D2）

> 角色：**P3 · 前端与交互**（`SPEC.md` §9.1）
> 负责目录：`frontend/`（全部）、`backend/app/tutor/`、`backend/app/api/qa.py`
> 排期定位：**实际开发第 1 天**。SPEC 原 D1 = 9/16，因前置的方案与规格工作占用一天，**整体偏移 1 天**（已登记于 R-01）。
> 今日目标：**D1 前端脚手架 + D2 素材工作台** —— 承接 M1「骨架跑通」中归 P3 的部分。

---

## 0. 先看两件事

### 0.1 依赖状态（I-3）

| 项 | 状态 |
|---|---|
| `docs/api-spec.md` v1.2（27 个端点） | ✅ **已是冻结态** → 前端可直接照它建模，不必等后端实现 |
| 后端骨架 / M1 | 🔄 由 P2 今日立起（`uvicorn` 可起、14 张表、27 端点骨架） |
| 前端 dev server 端口 | ✅ **5173**（已回执 P2，用于写开发期 CORS 白名单） |

> **接口冻结纪律**（SPEC §9.2）：`docs/api-spec.md` 冻结后，只允许**新增端点**与**新增可选字段**，不允许改已有字段名或类型。前端类型层与之严格对齐。

### 0.2 仓库与分支

| 项 | 值 |
|---|---|
| 工作仓库 | `multimodal-teaching-agent`（`main` = `c19acfa`，PR #1 已合并） |
| 本日分支 | `feat/frontend-scaffold`（遵守 [`CONTRIBUTING.md`](../../CONTRIBUTING.md)：不在 `main` 开发） |
| 提交规范 | 类型前缀 + 简短说明，不加人员前缀 |

---

## 1. 今日任务（按优先级）

### ★ 任务 1 · 前端脚手架与设计 token（对应 D1）—— ✅ 完成

- **技术栈**（SPEC §4.2 冻结，不可改）：React 18 + TypeScript + Vite 5 + TailwindCSS 3
- **路由**（api-spec §8）：`/` `/materials` `/graph` `/path` `/tutor` `/report`
- **设计 token**：难度色阶（1–5）、状态色、品牌色，集中于 `frontend/tailwind.config.js`
- **交付**：`frontend/` 全套工程配置（vite / tsconfig / tailwind / postcss / .gitignore / .env.example）

### ★ 任务 2 · API 客户端与类型层（对齐 api-spec 27 端点）—— ✅ 完成

- 统一响应包封 `{ok,data,request_id}` 与错误 `{ok,error:{code,message,detail}}`（api-spec §1.1 / §1.2）
- **8 个后端错误码** + 2 个前端本地码（`NETWORK_ERROR` / `BAD_RESPONSE`）；`error.message` 直接展示，绝不拼装英文
- **27 个端点全部封装**（`src/lib/endpoints.ts`）；SSE 流式接口（`/ask`）单独预留 `lib/sse.ts`（D8 实现）
- 上传支持格式/大小上限**从 `/api/meta/capabilities` 读取**，前端不硬编码

### ★ 任务 3 · 素材工作台页（D2 主交付）—— ✅ 完成

覆盖 SPEC §5.1 的功能点：

| 功能点 | 实现 |
|---|---|
| F1.1 上传 | 拖拽/点选多文件；`POST /api/materials`；**部分失败**（`accepted`/`rejected`）逐条展示可读原因 |
| F1.3 解析方式可见 | 清单列显示实际 `parse_method`（文本抽取 / 本地 OCR 等） |
| F1.6 素材清单 | 字段与 `Material` 一一对应：文件名 / 类型 / 大小 / 解析方式 / 页数 / 状态 / 质量分 / 存疑处 |
| F1.7 进度与失败可见 | `useJobsPolling` 只轮询未终态任务；展示中文 `stage_detail`；失败可**单独重试**，不阻塞同批 |
| 存疑处 | 行内展开明细（类型 + 页码 + 严重度 + 说明），落实「显式不确定性」产品原则 |

### 任务 4 · 五页骨架与整体布局 —— ✅ 完成

- 概览页（三阶段入口 + 产品原则）；知识图谱 / 学习路径 / 答疑辅导 / 质量报告为**骨架占位**
- 占位页只列「排期 + 将对接的端点」，**不提前实现 D6–D8 的功能**（严格按 §8.2 排期）

### 任务 5 · 构建验证 —— ✅ 完成

| 检查 | 结果 |
|---|---|
| `tsc --noEmit` | ✅ **零错误** |
| `vite build` | ✅ **成功**：1594 modules transformed，产出 `dist/`（JS 242.61 kB / CSS 16.43 kB，8.55s） |

---

## 2. 今天明确不做（避免提前实现）

| 不做 | 排期 |
|---|---|
| 知识图谱可视化（Cytoscape.js） | D7 |
| 学习路径页 | D7 |
| 答疑页 + SSE 流式 + 诊断面板 | D8 |
| 人工校验工作台 UI | D6 |
| 苏格拉底状态机后端 + 模板库 | D8 |
| Demo 视频 / PPT / 社媒连载 | D10 |

---

## 3. 需要向别人要的东西 / 给别人的回执

| 方向 | 内容 | 用途 |
|---|---|---|
| **→ P2** | dev server 端口 = **5173**；开发期代理 `/api → http://127.0.0.1:8000` | 写 CORS 白名单、联调 |
| **→ P2** | 后端接口一落地就通知；前端已按 v1.2 全量建模 | 即时联调 |
| **← P2** | I-3 端点冻结（已收到 v1.2） | ✅ 已就绪 |

---

## 4. 今日 EOD 自检

- [x] 前端工程可 `tsc --noEmit`（零错误）与 `vite build`（成功出 `dist/`）
- [x] 素材工作台页（上传 / 清单 / 轮询 / 存疑处 / 重试）实现完成
- [x] API 类型层覆盖全部 27 个端点
- [ ] **与后端真实接口联调**（等 P2 的 `/api/health`、`/api/materials` 落地）
- [ ] **推送分支并开 PR**：`git push -u origin feat/frontend-scaffold` → GitHub 开 PR，至少 1 名队友 review

---

## 5. 如果今天时间被压缩（只剩 2–3 小时）

按此顺序砍，**从下往上**：

| 保留优先级 | 任务 | 说明 |
|---|---|---|
| 1 | 前端脚手架 + 路由 + 设计 token（任务 1） | 没有这个前端无从谈起 |
| 2 | API 类型层 + 客户端（任务 2） | **P2 接口一冻结就要能接**，不可砍 |
| 3 | **素材工作台页**（任务 3） | D2 主交付，M1 的前端侧证据 |
| — | 图谱 / 路径 / 答疑 / 报告占位页 | 可延后（本就排期在 D6–D8） |

> 对比 P2 那份：P2 不可砍的是「27 端点骨架冻结」，P3 不可砍的是「类型层 + 工作台」——**两条线在 I-3 处咬合**。
