# 待办总览 · 2026-09-20（周日）

> **生成**：9/20 10:15　**距截止**：**6 天**（9/26 23:59）
> **来源**：远端分支 / 开放 PR / 开放 Issue 逐条核对，不是凭记忆

---

## 一、三人状态（实测）

| 人 | 最近动作 | 未合并分支 |
|---|---|---|
| **P1** DakerDack | **今早 09:00 前后合并 6 个 PR**（解析去噪 / PPTX / doctor.py / 文档同步）+ 开了 OCR 与章级标题两个分支 | `feat/parse-ocr`、`feat/parse-chapter-headings` |
| **P2** Mikeys5s（我） | 昨晚：假数据归零 / 四道闸 / 真 UI 上线 / 契约比对 / 复盘 / 会话归档 | —— |
| **P3** RyeYen | **昨晚灌了 3 份真实教材**（CC BY 4.0）+ 修了两个前端崩溃 + **写了 Issue #66** | `feat/frontend-scaffold`（含崩溃修复，**未合并**） |

**⭐ 两个关键变化**：
1. **素材问题解决了** —— P3 用**开放教材**（Systems Approach, CC BY 4.0）灌了 Ch3/Ch5/Ch6 三份（57+31+24 页，共 1,300+ 块）→ **Issue #55 可以关**
2. **P3 的 `feat/frontend-scaffold` 有两个崩溃修复未合并** → **线上还带着崩溃**

---

## 二、🔴 P2（我）的待办 —— 按优先级

### P0 · 阻塞 9/24 录制

| # | 事项 | 出处 | 为什么是 P0 |
|---|---|---|---|
| **1** | **修 `POST /api/extract/knowledge` 挂死** + **给任务加超时回收** | #66 §2 | 知识点 = 0 → `/graph` `/path` `/report` `/tutor` **四页全空**。扫描件实测 **14 分钟停在 progress=15**，且**永远留在 running** |
| **2** | **`/materials/{id}/outline` 换成按真实 blocks 推导** | #66 §3 | 三份不同材料返回**一模一样的 mock**（含"传输层"）→ **Stage 2 打底就是它** |
| **3** | **答疑链路按入参区分**（现在是 9/17 的 mock，**内容是快排，与学科不符**） | #66 §4 | ⚠️ **视频会穿帮**：镜头里计算机网络材料 → 跳出快速排序 |
| **4** | **`/api/*` 不能返回 SPA HTML + 200** | #66 §7 | 演示现场若撞上，**六页齐崩且状态码是 200**，错误处理兜不住 |

### P1 · 演示质量

| # | 事项 | 出处 |
|---|---|---|
| **5** | `/report/quality` 的 `acceptance` 在**样本为 0 时不要报 `passed=true`** | #66 §6、**#58 模式 B** |
| **6** | `seq` **跨轮次不重置** | #66 §5.2 |
| **7** | **`/learning-path` 收口为顶层数组**（P3 已在 Issue #15 评论里点名要我改） | #15 评论、P3 代码注释 |

### P2 · 我自己的账

| # | 事项 | 出处 |
|---|---|---|
| **8** | **闸③ 的缺口：只打 GET + 端点列表写死** → **正是它漏掉了 `/outline` 的假数据** | 昨天我自己记的，**今天被 P3 证实** |
| **9** | `pyproject.toml` 里 `pymupdf`/`python-docx`/`python-pptx` **重复声明**（基础依赖 + `parse` extra 都有），注释已过期 | 昨晚的部署修复 |
| **10** | **把交付清单接进 `gate.sh`** —— 让"计划项没做"自动报出来 | 昨天的建议 |
| **11** | 契约比对**字段层面**仍空白（只有路径+方法）→ **P3 抓的两个崩溃都是字段/形状问题** | 昨天预判 + 今天被证实 |

---

## 三、🔴 美佳（人的操作）

| # | 事项 | 为什么只能人做 |
|---|---|---|
| **1** | **仓库转 public** —— Settings → General → Danger Zone → Change visibility | **匿名请求已确认仍是 private**。交付前必须转，否则**评委打不开、源码仓库这项作废** |
| **2** | **合并 P3 的 `feat/frontend-scaffold`**（或让 P3 合） | 含两个崩溃修复，**线上还带着** |
| **3** | **P1 的 `feat/parse-ocr` / `feat/parse-chapter-headings` 两个 PR** | 他在推，需要 review |
| **4** | **PPT / 视频脚本分工** | #52 已排：9/21 脚本初稿、9/22–23 保底提交、9/24 录制 |

---

## 四、⚠️ P3 给了一个**必须现在回答**的问题

> **"§8 请求 1 之外的四条，只需要你回一句『什么时候能给』"** —— 他要拿去对齐录制排期。
>
> 他的顾虑很实在：**"演示脚本 9/21–9/23 就要写完，脚本得知道那天页面上会出现什么。"**
> 而且他准备了一个**保底方案**：
> **"演示 Stage 1 全链路（真实）+ Stage 2/3 用预置数据讲解，至少不会在镜头前露出『计算机网络材料 → 快排知识点』这种穿帮。"**

**我欠他一个回答。**

---

## 五、今天的建议顺序

1. **先回 #66**（给 P3 时间承诺，他卡在脚本上）
2. **修 1（挂死）** —— 它是四页全空的根因
3. **修 2、3（outline / 答疑 mock）** —— 视频内容质量
4. **修 4（SPA 抢路由）** —— 演示稳定性
5. **闸③ 缺口（8）** —— 它是**这次漏检的原因**，不修还会漏
