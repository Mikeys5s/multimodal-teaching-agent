# Demo 视频脚本（3 分钟）· **定稿 v2**

> **主责**：P3（`docs/deliverable-plan.md`）　**录制日**：9/24　**保底提交**：9/25
> **定稿依据**：SPEC §4.8 / D-15、`docs/deliverable-plan.md` 四条纪律、SPEC 定稿的主创新点措辞
> **素材口径**：Stage 1 / Stage 2 / Stage 3 **全部真实**（9/21 起答疑不再是 mock；9/22 起库里有 15 条 `hard` 边）
> **v2 相对 v1 的三处变更**：
> 1. **环节 5 定稿走 A 方案**（`hard` 候选边已入库，`hard_edge_count = 15`）—— 不再走降级演法
> 2. **新增 15 秒「AI 判反、人判正」**（用被环校验排除的那条边 `…016 → …011`），纪律④ 从"采纳一条边"升级为"人发现 AI 的系统性偏差"
> 3. **总时长重新配平**（原 180 秒零余量 → 现在分段留白，另附 §6 语速核对表）

---

## 0 一句话口径（全片不许偏离）

> 主创新点（**SPEC 定稿原文**）：
> 「**AI 预抽取 + 人工校验的知识点依赖图构建**，并首次把『**教学依赖图必须无环**』作为工程不变量强制保证」
>
> 答辩口径：**「AI 干粗活 + 人做裁决」**

🔴 **禁止出现的说法**：
- ❌「全自动抽取」/「AI 自动生成知识图谱」
- ❌「我们来自不同专业」（三人同属人工智能专业，加分项确定不满足，提了等于暴露短处）
- ❌ 任何"模型在线推理"的暗示 —— 本项目**运行期零模型依赖**，AI 只在构建期离线干活

---

## 1 分镜表（总长 180 秒）

| # | 时间 | 秒 | 画面 | 动作 | 台词（可直接念） |
|---|---|---|---|---|---|
| 1 | 0:00–0:10 | 10 | 首页 | 停在首页，展示六个入口 | 「学生的第一个问题永远是：我该从哪学起、学不动的时候卡在哪。析知就是为这件事做的。」 |
| 2 | 0:10–0:36 | 26 | `/materials` → 预览抽屉 | 展示素材清单（3 份真实教材）→ 打开 Markdown 预览 → **点页码锚点跳回原文** | 「第一步，把教材变成可被引用的材料。注意这不是截图 —— 每个段落都带页码锚点，点一下就回到原文。后面每一条结论、每一条依赖，都能追到这一页。三份材料来自开放许可的英文原版，真实、可复现。」 |
| 3 | 0:36–0:58 | 22 | `/materials` 章节骨架 | 打开 outline，逐份点过去 | 「解析完就有章节骨架。三份材料的章节数是 71、35、31，各不相同 —— 如果两份材料返回一样的结构，那就说明数据是假的。章节归属不只是好看：它决定每条知识的溯源，也决定依赖图的边界。」 |
| 4 | 0:58–1:28 | 30 | `/graph` | 拖/缩放图；指 stats 面板 | 「然后是核心：知识点依赖图。629 个知识点、638 条依赖边。请留意这行 —— 环数 0。教学依赖图必须无环：A 是 B 的前置、B 是 C 的前置，C 就不能再是 A 的前置，否则学生永远学不动。我们把这条当成工程不变量来保证，不是画完图再检查。」 |
| 5a | 1:28–1:53 | 25 | `/graph` → 点「人工校验」 | 打开复核抽屉 → 挑一条**硬前置**边 → 点「**采纳进正式图**」→ 关抽屉，角标变少 | 「候选边是 AI 在构建期判出来的，但候选不等于结论 —— 它先进入复核队列。这里有一条硬前置，人看过原文之后点采纳，它才进入正式图。这就是所谓『AI 干粗活、人做裁决』。」 |
| 5b | 1:53–2:13 | 20 | 复核抽屉 → `/path` | 切到 `/path`，指路径顺序；再切回复核队列展示那条**被排除**的边 | 「再看这条：AI 按文档出现顺序抽成了『滑动窗口 → TCP』，人读完原文判断方向正好相反 —— 得先懂 TCP 的字节流，才谈得上滑动窗口。我们没把它悄悄改掉，而是留在队列里**等人裁决**。」 |
| 6 | 2:13–2:27 | 14 | `/path` | 选知识点 → 展示拓扑学习路径 | 「人确认过之后，学习路径真的变了 —— 这条依赖现在约束了先后顺序。学生的前置没掌握，系统能顺着这条边**反向回溯到卡点**。」 |
| 7 | 2:27–2:53 | 26 | `/tutor` | 提一个**计网**问题 → 看**流式**回答 + 溯源卡片 | 「最后是答疑。它不直接给答案 —— 它先反问你一个问题，用苏格拉底式提示让你自己说出来。旁边这张溯源卡片说明：它只在这份教材里找依据，找不到就直说找不到，不做推测补全。」 |
| 8 | 2:53–3:00 | 7 | `/report` | 停在这一页 | 「所有指标都带样本量 —— 不是"通过"，是**有分母的通过**。谢谢。」 |

**⚠️ 时间是最紧的约束**：§6 是逐段语速核对表，**录前按它念一遍**。
超时的第一刀砍**环节 3**（章节骨架），第二刀砍**环节 6**（学习路径）—— 它们能被环节 4 / 5 的台词顺带带过。
**⛔ 不许砍环节 5b** —— 它是本片最有说服力的一镜，也是纪律④ 的落点。

---

### ⭐ 环节 5：**定稿演 A 方案**（v1 的 A/B 二选一已收口）

| | **方案 A（✅ 定稿采用）** | ~~方案 B（降级）~~ |
|---|---|---|
| **前提** | 队列里**有 `hard` 候选边** —— **9/22 已满足**：`hard_edge_count = 15`，复核队列里 hard 候选 13–14 条 | 仅在"部署后 hard 候选消失"这种异常下回退 |
| **画面** | 抽屉里挑带「硬前置」标签的边 → 采纳 → 切到 `/path` 指路径变长 | 采纳一条 soft → 只指角标减少 |
| **强度** | ⭐⭐⭐ **看得见的因果** | ⭐⭐ 只有动作没有因果 |

**🔴 开录前 30 秒的检查**（一条命令，别靠记忆）：
```
GET /api/review/queue?limit=500   → 数一下 relation_type == "hard" 的条数
  ≥ 1  → 走 A（定稿）
  = 0  → 说明部署后数据又回退了，**当场改走 B 并改台词**（不许说"路径变了"而实际没变）
```
⚠️ `limit` 上限是 **500**（1000 会 400 `INVALID_PARAM`）。

---

## 2 录制前的数据准备（逐条打勾，缺一条就会当场卡住）

| # | 准备项 | 怎么确认 |
|---|---|---|
| 1 | 演示库里 **3 份真教材**在位（Ch03 / Ch05 / Ch06） | `/materials` 列表可见；**别动它们**（P2 的回归基线） |
| 2 | 知识点 **629** 条、依赖边 **638** 条、**环数 0**、**`hard` 15 条** | `/report` 的样本量 + `/graph` 的 stats |
| 3 | **复核队列里有 `hard` 候选边**（脚本里写死的是 13–14 条） | 抽屉里按「硬前置」标签找 |
| 4 | ⭐ **线上已部署 PR #110**（`/path` 话术修复） | 见 §2.1 —— **没部署就别录环节 6** |
| 5 | `/tutor` 能建出**新会话**（会话 id 不是固定常量） | 建一次看 id 是否变化 —— **固定 id = 旧 mock 又回来了** |
| 6 | 想好的**提问句**与材料同学科 | 台词里的问题必须能在 Ch03/05/06 里检索到 |
| 7 | 浏览器**缩放 100%**、隐藏书签栏、关掉通知 | —— |
| 8 | 提前清掉 `/materials` 里的 **failed 夹具行**（`a.pdf` / `第5章.pdf`）或录时避开 | 镜头扫到"失败"两行不好看（已在 Issue 请 P2 处置） |
| 9 | ⭐ **取景避开重复段**（P2 提醒）：图上同一标题会连排 4 次 | 环节 4 用**缩放后的全景**，别近景怼单个节点 |

### 2.1 ⭐ 为什么"必须先部署 #110"

`/path` 页原先有一句**已被我们判定为错**的话：「这个知识点在依赖图里是**可以直接学起的起点**」。
`hard` 边为 0 时它确实是"起点"，但**现在库里已经有 15 条 hard 边** —— 这句话在镜头里是**自相矛盾**的。
PR #110 已把措辞改成**不下断言**的版本（「未检出硬前置 —— 但这不等于它可以直接学起」），
**但 9/23 09:00 实测线上 bundle 仍是部署前的版本**（仍含旧断言、不含新话术）。

**⇒ 录制入口条件：线上 bundle 里含「未检出硬前置」。** 未满足就去找 P2 部署，别硬录。

---

## 3 风险与兜底（镜头前出问题时的处理）

| 风险 | 现场怎么办 |
|---|---|
| 某页请求失败 / 白屏 | **别停在错页解释**。直接切下一环节，台词用"这一页的数据这里是…"带过。**兜底：开录前把每页截图备好，剪辑时替换**（P2 已有 12+ 张） |
| `/tutor` 回答质量差（提问落在噪声知识点上） | **提前用 3–5 个问题试跑一遍**，挑回答最稳的写进台词；台词里不要出现"随便问一个" |
| 采纳边之后 `/path` 变化不明显 | 剪辑做**前后对比**（同视角录两次），或改指 stats 面板里 hard 边数量变化 |
| 录到一半发现超时 | 按 §1 砍单顺序：**先砍环节 3、再砍环节 6；环节 5b 不许砍** |
| 复核抽屉打开是空的 | 说明部署后队列被清过 → 当场改走 **B**，并按 B 的台词念 |

---

## 4 与"五分钟版"的分工（如果最终允许更长）

若评委允许超过 3 分钟，**只加一段**：`/graph` 的**双通道对比** —— 同一条依赖边由"结构线索"与"语义通道"分别给出，两条都支持时标 `both`（现在库里 15 条 hard 边里有 1 条就是 `both`）。这是"双通道"最有说服力的展示，**3 分钟版放不下**。

---

## 5 🔴 录制纪律（来自 `docs/deliverable-plan.md`，逐条照做）

1. **先写脚本再录** —— 本文件就是它。**录之前按 §6 掐一遍表。**
2. **PPT 里禁止出现「我们来自不同专业」**（本片同理）。
3. **主创新点用 SPEC 定稿那句**（见 §0），**不说"全自动抽取"**。
4. ⭐ **视频里必须演「人工复核」那一步** —— 就是环节 5：**候选边 → 人采纳 → 学习路径随之变化**；并**加演 5b「AI 判反、人判正」**。这是"人做裁决"的可视证据，也是本片最重要的一镜。

---

## 6 逐段语速核对表（**"掐表预演"的代理，不是真念**）

> 口径：中文讲解的**舒适语速约 200–240 字/分钟**（每秒 3.3–4.0 字）。
> 下表按台词**字数 ÷ 分配秒数**算，**> 240 字/分钟即标红**（念不完或会赶）。
> ⚠️ 本表是**估算**：它只能证明"台词与秒数配得上"，**不能替代开录前空口念一遍**（念是唯一能听出"绕口"的方法）。

| # | 时段 | 秒 | 台词字数 | **语速（字/分钟）** | 判定 |
|---|---|---|---|---|---|
| 1 | 0:00–0:10 | 10 | 36 | **216** | ✅ 舒适 |
| 2 | 0:10–0:36 | 26 | 79 | **182** | ✅ 舒适 |
| 3 | 0:36–0:58 | 22 | 78 | **213** | ✅ 舒适 |
| 4 | 0:58–1:28 | 30 | 96 | **192** | ✅ 舒适 |
| 5a | 1:28–1:53 | 25 | 69 | **166** | ✅ 舒适 |
| **5b** | **1:53–2:13** | **20** | **74** | **222** | ✅ 舒适 |
| 6 | 2:13–2:27 | 14 | 50 | **214** | ✅ 舒适 |
| 7 | 2:27–2:53 | 26 | 72 | **166** | ✅ 舒适 |
| 8 | 2:53–3:00 | 7 | 22 | **189** | ✅ 舒适 |
| **合计** | 0:00–3:00 | **180** | **576** | **192** | —— |

**结论：9 段全部落在 148–222 字/分钟，没有一段超速。**

⚠️ **这条结论的来历值得写下来**（免得下次重复踩）：
第一版配平时 **5b 段是 14 秒 / 74 字 = 317 字/分钟 → 🔴「必删（念不完）」**。
**如果只按时长表分配而不核字数，这一镜到了现场只能靠加快语速硬顶，或者当场砍掉——而它恰恰是纪律④ 的那一镜。**
改法不是删台词，而是**从有富余的段落各匀 1–3 秒**（环节 2 −2s / 5a −3s / 8 −1s），把 5b 从 14 秒抬到 20 秒。

⛔ **仍未闭环的一点**：本表只能证明"台词与秒数**配得上**"。
**它不能替代开录前空口念一遍** —— 念是唯一能听出"绕口、换气不够"的方法。**9/23 预演/9/24 录前各念一次。**

---

## 7 待办 / 待确认（写在脚本里，避免录的时候才想起来）

- [x] ~~环节 5 的抽屉 UI 需先合并~~（PR #86 已合）+ ~~线上未部署~~ → **9/21 16:4x 已部署**，抽屉文案已在 bundle 里
- [x] ~~环节 5 必须真跑一次~~ → **数据层已干跑**：队列里有 13–14 条 `hard` 候选（本次定稿的依据）
- [ ] ⭐ **等 P2 重新部署（含 #110）** → 判据见 §2.1
- [ ] **等 P2 处置 2 份 failed 素材**（`a.pdf` / `第5章.pdf`）
- [ ] 台词里的**提问句**用线上真实材料试跑定稿
- [ ] 环节 4 的 stats 数字（**629 / 638 / 环数 0 / hard 15**）**录前现查一遍**，不要照抄本文件（数据会变）
- [ ] **带前端点击真跑一次环节 5**（采纳 → 路径变化的前后截图）—— 本机无浏览器，**已委托 P2**
- [ ] 是否加**中文字幕**（英文材料出现在画面里，字幕能显著降低评委理解成本）
