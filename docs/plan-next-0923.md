# 下一步计划 · 9/23 傍晚

> 生成 2026-09-23 18:25 · **距 9/26 23:59 还有 3 天 5.5 小时** · 功能冻结 9/24 EOD
> 依据：刚完成的「例题/误区」灌库实测结果

---

## 一、先说刚做完什么（**都已验证，不是"应该好了"**）

```
灌库：kp_examples 10 行 / kp_misconceptions 7 行
接口：10 / 10 个点能读出内容（例 2 误 1 / 例 1 误 2 …）
字段：例题 = short_answer + source_page；误区 = source='human'
```

**17 条内容全部带 `source_quote`（从材料原文直接摘的）**，材料覆盖：子网划分 / 校验和 / 三次握手 /
AIMD / 慢启动 / 距离向量 / 链路状态 / LSP 序号 / IP 分片 / VPN。

---

## 二、🔴 但有一个必须先澄清的口径问题

```
全库 629 个点里：带例题 9 个（1.4%）  带误区 6 个（1.0%）
```

**A2-2 写的是「五要素字段完备率 ≥ 95%」**，而**例题/误区是五要素之一**。
**若按"每个点都必须有例题"算，我们现在差得远。**

**但 SPEC L712（F2.2）自己给了另一条路**：

> **「缺失的字段显式标 `待核实` 而非留空」**

**⇒ 所以 A2-2 有两种读法，结论完全相反**：

| 读法 | 判据 | 我们现在 | 要做什么 |
|---|---|---|---|
| **甲：非空率** | 每个点的例题/误区字段**必须有实际内容** | **1.4%** ❌ | 要么补 600 条（**撞幻觉红线**），要么**改口径** |
| **乙：显式标注率** | 非空 **或** 显式写 `待核实` | **可以做到 100%** ✅ | **给剩余 620 个点批量写 `待核实`** |

**⭐ 我倾向乙，理由**：
1. **它是 SPEC 自己写的**（L712），**不是我为省事找的借口**
2. **甲在 3 天内不可能达成** —— 除非生成 600 条，**而那直接撞"幻觉率 0"的红线**
3. **乙反而更诚实** —— **"我们知道这里缺，标出来了"比"编 600 条填满"更符合产品理念**

**⚠️ 但这是**口径**问题，得你或 P1 拍板** —— 我不自己定。

---

## 三、待办（**按"能不能今天做完"分**）

### 🟢 我这边，今天能做完（约 1.5 小时）

| # | 待办 | 预估 | 依赖 |
|---|---|---|---|
| **A1** | **定 A2-2 口径**（甲/乙）—— 见上，**要你或 P1 一句话** | —— | **你** |
| **A2** | 若走乙：**给剩余 620 个点写 `待核实`**（一条 SQL / 脚本） | 30 min | A1 |
| **A3** | **前端验证**：打开 `/path`，看面板真显示了例题/误区 | 10 min | —— |
| **A4** | **更新 `templates.py` 的那句注释**（写着"表为空"，现在不空了） | 10 min | —— |
| **A5** | **提交**（JSON + seed 脚本 + 验证脚本进仓库） | 15 min | —— |
| **A6** | **给 P3 一条**：环节 6 现在有真实例题可展示 | 5 min | —— |

### 🟡 你这边（**交付物，最要紧**）

| # | 待办 | 为什么它最要紧 |
|---|---|---|
| **B1** | **导出 LearnBuddy 历史对话记录** | **五项交付物里唯一"完全没开始"的**，不可控性最高（可能要审核/转格式） |
| **B2** | **验收 PPT v2** | 有意见我才能改，**改完还要留时间给你复审** |

### 🔵 P3 那边

| # | 待办 | 状态 |
|---|---|---|
| **C1** | 录制（脚本 / 数据 / 部署**都就绪了**） | **无阻塞** |
| **C2** | 回 `#94`（我昨天报了部署完成 + 三层判据） | —— |

---

## 四、⭐ 我建议的顺序（**给理由，不是罗列**）

| 序 | 做什么 | 一句话理由 |
|---|---|---|
| **1** | **A1：你回一句 A2-2 走甲还是乙** | **它决定我要不要花 30 分钟做 A2** —— 而不回，我就卡在这 |
| **2** | **B1：你导对话记录** | **唯一"零进度"且不可控的交付物** |
| **3** | **A3+A4+A5：我这边收尾**（前端验证 / 注释 / 提交） | 都是 10–15 分钟的小事，**但做完"例题/误区"这条线才算闭环** |
| **4** | **B2：你验收 PPT** | 排第 4 是因为 **A 线已经把我的活清了**，你的两件里 B1 更紧 |

---

## 五、我**不建议**现在做的

| 事 | 为什么不做 |
|---|---|
| **补 600 条例题填满 629 个点** | **撞"幻觉率 0"红线** —— 而且 3 天做不完 |
| **改抽取管线**（让它自动生成例题） | 功能冻结 9/24，**动抽取是最贵的地方** |
| **为 A2-2 改 SPEC 的 95%** | **SPEC 是唯一权威源，不能为了达标改标准** —— 要么补数据，要么用 L712 的口径 |
| **把例题/误区加进 PPT** | PPT v2 你还没验收，**加内容会打乱你的验收节奏** |

---

## 六、一句话总结

**A 线（例题/误区）只剩「口径 + 收尾」，半小时内能关档；
真正的瓶颈在你手上的两件交付物 —— 尤其那份对话记录。**
