# 待办与计划 · 2026-09-20 14:50

> **距截止 6 天**（9/26 23:59）· **距录制 4 天**（9/24）
> 来源：远端提交 / 开放 Issue / 线上实测 —— **逐条查过，不是凭记忆**

---

## 一、三人状态（截至 14:50）

| 人 | 今天的动作 |
|---|---|
| **P1** DakerDack | **9 个提交**：解析去噪 / **PPTX 解析** / **`doctor.py`（.git 一体检修复）** / `[parse]`-`[ocr]` 文档同步 / review 落实 / **章级标题去污染**（#37 已合并）。剩 `feat/parse-ocr` 未合并 |
| **P2** 我 | **8 个提交 / 5 个 PR**：extract 挂死 / SPA 断言 / acceptance 样本0 / learning-path 收口 / **outline 真实推导** / **知识点名清洗 + 方案 B** / **部署后自检** |
| **P3** RyeYen | **3 个提交**：两个前端崩溃修复（`/learning-path` 形状、`capabilities` 漂移）+ 待办总览。**都已合并** |

**未合并分支**：`feat/parse-ocr`（P1）、`chore/venv-outside-project`

---

## 二、⭐ 线上现状（实测）

```
知识点 627   依赖边 624   环数 0
三级结构完整率 1.0   溯源覆盖率 1.0
三份材料的 outline：31 / 35 / 71 章，各不相同
答疑轮次 0（幻觉率的样本还是空的）
```

**今天的两条主线都通了**：
- **Stage 1（解析）**：真实，三份教材各 24–57 页
- **Stage 2（结构化）**：**真实**，627 知识点 / 624 边 / 环数 0 / outline 真实

**❌ Stage 3（答疑）还是 mock —— 而且是"快排"的内容。**

---

## 三、待办（按优先级）

### 🔴 P0 · 卡住"可演示"

| # | 事项 | 为什么 |
|---|---|---|
| **1** | **答疑链路按入参区分**（现在是 9/17 的 mock，**内容是快排**） | ⚠️ **视频会当场穿帮**：计算机网络材料 → 跳出快速排序。P3 反复提过这条 |
| **2** | **`seq` 跨轮次不重置** | #66 §5.2，断线续推会错 |

### 🟡 P1 · 让"人工兜底"真正可做

| # | 事项 | 为什么 |
|---|---|---|
| **3** | **导出 627 个知识点的候选清单**（给 P3 挑前 ~50 个用于演示） | 你拍板要人工兜底。627 个挑得出来，但**需要一个清单**，不能在页面上翻 |
| **4** | 残留噪声（`Victory` / `The next issue` 这类被解析器当标题的引语） | 数量少，**清洗规则可再收紧一点**，或直接由 #3 的人工挑掉 |

### 🟢 P2 · 工程债（不做会继续漏东西）

| # | 事项 | 为什么 |
|---|---|---|
| **5** | **闸③ 补缺口**（只打 GET + 端点列表写死） | **这次漏掉 `/outline` 假数据就是它** |
| **6** | **契约比对补"字段层"** | P3 抓的两个崩溃（形状、字段漂移）**我的工具都抓不到** |
| **7** | **本机 venv 装不上依赖 → 全量测试跑不了** | P1 开了 `chore/venv-outside-project`，**也许正是解这个** —— 要问他 |

### ⚪ P3 · 只有人能做

| # | 事项 | 为什么 |
|---|---|---|
| **8** | **仓库转 public** | 匿名请求确认仍是 private。**交付前必须转，否则源码仓库这项作废**（我提了三次，你还没点） |
| **9** | review / 合并 **P1 的 `feat/parse-ocr`** | 他在推 |
| **10** | **9/22 保底版是否含视频** —— 你已定「含」 | 那意味着**视频要在 9/22 前有个草稿** |

### 📦 P4 · 交付物（**三项全空**）

| # | 事项 | 最晚 |
|---|---|---|
| **11** | **演示脚本**（P3 在等页面定稿 —— **现在可以定了**） | 9/21 |
| **12** | **PPT 大纲 + 初稿** | 9/22 |
| **13** | **LearnBuddy 对话记录导出** | 9/21 |
| **14** | **视频**（含草稿，因为你定了 9/22 保底版含视频） | 9/24 定稿 / **9/22 草稿** |

---

## 四、⭐ 现在可以给 P3 一个明确答复了

P3 在 #66 里说：**「演示脚本 9/21–9/23 就要写完，脚本得知道那天页面上会出现什么」**，
并且准备了一个保底方案（Stage 1 真实 + Stage 2/3 预置）。

**现在的实际状态**（可以直接告诉他）：

| Stage | 状态 | 视频里能怎么演 |
|---|---|---|
| **Stage 1 解析** | ✅ 真实 | **全真实** —— 上传 PDF、出 Markdown、出素材清单 |
| **Stage 2 结构化** | ✅ **今天刚从 mock 变真** | **全真实** —— 627 知识点、依赖图、学习路径、章节大纲 |
| **Stage 3 答疑** | ❌ **仍是 mock（快排内容）** | **暂时只能"预置数据讲解"** |

**→ 他现在可以按「Stage 1 + Stage 2 全真实，Stage 3 预置」写脚本。**
**我明天把 Stage 3 做完，再升级成全真实 —— 升级比降级容易。**

---

## 五、计划

```
9/20 今天下午   ① 答疑 mock 换掉（P0-1）—— 视频穿帮风险最高的一条
                ② 导出知识点候选清单（P1-3）给 P3 挑
                ⭐ 同时：告诉 P3「页面可以定了」

9/21            ① seq（P0-2）
                ② 内容侧：演示脚本初稿 + 对话记录导出
                ⭐ 交付物开始有实物

9/22            ① 提交保底版（含视频草稿 —— 你定的）
                ② PPT 初稿
                ③ 产品：Stage 3 若做完则升级脚本

9/23            脚本定稿 + PPT 定稿 + 工程债（闸③ / 字段层）

9/24            录制期：视频 + PPT 定稿

9/25            五项齐备 → 提前提交一版

9/26 23:59      最终提交
```

---

## 六、我认为最要紧的三件

| # | 事项 | 我的建议 |
|---|---|---|
| **1** | **仓库转 public** | **今天就转**。我提了三次，它零技术含量但是"作废"级风险 |
| **2** | **答疑 mock** | **今天做掉** —— 它是**唯一还会在镜头前穿帮**的地方 |
| **3** | **P3 的脚本可以定了** | 现在页面形态明确了，**别让他继续等** —— 他等一天，内容期就少一天 |
