# 素材请求 · 请提供一份用于演示与验收的真实教材

**发起**：@Mikeys5s（P2）
**日期**：2026-09-19
**优先级**：**高 —— 这是视频录制与验收指标的前置，没有任何技术手段能替代**

---

## 一、为什么现在就要（不是"顺便提一下"）

今天盘点了手上所有素材：`backend/uploads/` 里 **20 个 PDF，全是 1-2 页、200 字左右的测试小文件**
（都是当初跑通路时随手传的）。**没有一份能当演示材料用。**

这件事同时卡住**三件**：

| 被卡住的 | 具体表现 |
|---|---|
| **① 验收指标没有信息量** | 现在 `A2-1 / A2-3 / B1-2 / B1-5` 都是 100%，但**数据量太小，是「真空满足」** —— 比率按定义就成立，证明不了任何事 |
| **② Demo 视频没法演** | 3 分钟要展示「上传 → 解析 → 知识点 → 依赖图 → 路径 → 答疑」，**演 1 页 PDF 会被一眼看穿** |
| **③ SPEC 的 V-12 一直没闭环** | 「知识骨架需用手上实际教材核对」—— 从 D2 记到现在，**始终没有教材可核对** |

**离 9/26 只剩 7 天，而视频的"内容期"9/21 就该开始了。**

---

## 二、需要什么（就一份，不挑）

**一份 PDF 即可**，满足：

- ✅ **有文本层**（不是扫描件）——  扫描件会走 OCR 链路，识别噪声会让演示效果打折扣
- ✅ **20 页以上**（太少撑不起"三级结构"的演示）
- ✅ **是计算机网络相关章节** —— 第 4 章（网络层）或第 5 章（传输层）最好，与 `SPEC §2.6` 的知识骨架对得上
- ✅ **能公开用于比赛演示**（教材版权请注意 —— 如果是受版权保护的正规教材，**只用于内部开发与验收，不放进公开仓库**）

> **如果你手上只有扫描版也没关系** —— 说一声，我们走 OCR 链路（那本来也是设计里的一条路），
> 只是要多留一天做人工校对。

**如果你手上就是 P1 做 Stage 1 时用的那份，直接给那份最好** —— 你已经验证过它能跑通。

---

## 三、放到哪里

**方式 A（推荐）**：直接把文件丢到仓库外的共享位置，告诉我路径 —— 我灌进演示库

**方式 B**：传到我这边，我用 `POST /api/materials` 灌进线上库

> ⚠️ **不要直接提交进仓库** —— `backend/uploads/` 已经在 `.gitignore` 里，
> 而且受版权保护的教材**不适合放进公开仓库**（仓库交付前要转 public）。

---

## 四、拿到之后我会做什么

1. 灌进**线上演示库**（`http://120.77.177.171:8000`），不是本地
2. **重跑一次验收指标**（`scripts/evaluate.py --strict`）—— 这次是**有信息量的 100%**
3. **把"演示用的检查点"整理给 P3** —— 视频脚本要照着真实数据写：
   - 哪一章 / 哪一节适合当"知识点抽取"的画面
   - 哪一对知识点适合演示"依赖图上的 hard 边"
   - 哪一条学习路径适合当"从地基到目标"的演示
4. 把这份素材登记进 `docs/deliverable-plan.md` 的前置清单

---

## 五、一句话版本（可以直接转给 P1）

> **P1，你之前做 Stage 1 解析时用的那份计算机网络教材 PDF 还在吗？
> 发我一份（有文本层的那种）。线上演示库现在全是 1 页的测试文件，
> 验收指标和视频都靠它。放哪都行，给我个路径。**
