# 待办 · 补「典型例题」与「常见误区」（例题/误区面板是空的）

> 生成：2026-09-23 18:00 · 依据：线上接口实测 + `backend/app/tutor/templates.py` 注释
> **距 9/26 23:59 还有 3 天 6 小时** · 功能冻结线 9/24 EOD

---

## 一、先讲清现状（**这三个字段全库都是 0，不是个别点漏了**）

| 查的 | 结果 | 怎么看出来的 |
|---|---|---|
| `kp_examples` | **全库 0** | 抽 24 个点 → `example_count` 分布 `{0: 24}`；再扫 60 个点，`examples` 数组全空 |
| `kp_misconceptions` | **全库 0** | 同上，`misconception_count` 分布 `{0: 24}` |
| **代码里早就写明** | —— | `templates.py`：**「不产 `kp_misconceptions` / `kp_examples` —— 那两张表需要【单独的构建步骤】」** |
| 前端 | **不用改** | `GapAnalysisPanel.tsx` 读的就是这两个字段（面板做对了，是数据没有） |

**⭐ 所以这不是 bug** —— 是**从来没做过那个"单独的构建步骤"**。

**⚠️ 而它有一个连带后果**：`tutor` 链路里 `probe` / `confirm` 两个招式**一直在用降级方案**
（`probe` 用知识点自身、`confirm` 用同节相邻点）—— **降级是明写的、不撒谎，但效果弱于真例题/真误区。**

---

## 二、⭐ 最重要的一个约束：**教材里没有现成习题**

```
629 个节点里，`Problem: ...` 开头的只有 3 个：
   Problem: Allocating Resources       （Ch06）
   Problem: Not All Networks are       （Ch03）
   Problem: Getting Processes to       （Ch05）
   ⇒ 每份材料各 1 个，是【章节的问题引言标题】，不是习题
```

**⇒ 所以"从材料里直接抽例题"这条路走不通** —— 只能**派生**或**生成**。

---

## 三、三条路（**给权衡 + 推荐**）

| | **A · 从材料派生** | **B · 人工撰写** | **C · LearnBuddy 平台生成** |
|---|---|---|---|
| **做法** | 把「问题引言 + 定义句」改写成本题型的例题；误区从**结构线索**派生（如拿同类不同义的两点做"易混"） | 我按材料**人工写**，逐条标原文出处 | 用 LearnBuddy 平台在构建期生成，人工校 |
| **可溯源** | ✅ 能指到原文行 | ✅ 能指到原文行 | ⚠️ 要人工补 |
| **真实性** | ✅ 不编造（都是材料里的话） | ✅ 最准 | ⚠️ **有幻觉风险** —— 与"幻觉率 0"红线冲突 |
| **成本** | 中（要写派生脚本） | **高**（纯人力） | 低 |
| **风险** | 派生得生硬（"问题引言"不是习题） | 时间不够 | **生成的内容材料里没有 ⇒ 溯源覆盖率会掉** |

### ➡️ 我的推荐：**A + B 混合，且小规模**

**具体做法**：

| 步 | 做什么 | 量 |
|---|---|---|
| **1** | **A 派生**：对**有 `Problem:` 引言的 3 个点**做真例题（材料里有依据） | **3 条** |
| **2** | **B 人工写**：在**每份材料的核心节**各挑 3–4 个点，人工写例题 + 误区，**逐条标页码** | **10–12 条** |
| **3** | **灌库 + 前端确认** | —— |

**总量 13–15 条** —— **覆盖 3 份材料的核心知识点，足够演示面板有内容**。

**⭐ 为什么不铺满 629 个点**：
1. **时间只剩 3 天**，而功能冻结在 9/24
2. **铺满 = 大量生成 = 幻觉风险**（**直接撞"幻觉率 0"的红线**）
3. **15 条精挑的，比 600 条可疑的更有说服力** —— 而且**答辩时可以说"我们只做了核心节，其余在复核队列"**

---

## 四、🔴 我要先说清的两个代价（**别等做完才发现**）

### ① 它会影响 D9 的验收表

`kp_examples` / `kp_misconceptions` 进库后，**A2-* / B2-* 里若有"例题覆盖率"这类指标，分母会变**。
**⇒ 做之前要先确认 D9 的验收表里有没有这两项**（我没有逐条看过 D9 表，**这条待确认**）。

### ② 它不是"补一个字段"，是"补一条链路"

这两张表要能被**读出来**（`kp_view.py` 已有 20 处读的代码 ✅）+ 能被**前端面板显示**（✅ 已就绪）
—— 所以缺的**只有数据**，链路是通的。**这点是好消息。**

---

## 五、待办清单（**按顺序，标了谁做**）

| # | 待办 | 谁 | 依赖 | 预估 |
|---|---|---|---|---|
| **1** | **确认 D9 验收表里有没有"例题/误区覆盖率"指标** | 我（查 SPEC §7） | —— | 10 min |
| **2** | **定"做哪些点"**：3 份材料各挑 3–5 个核心点（附节名） | 我 + 美佳过一眼 | 1 | 20 min |
| **3** | **写派生/撰写脚本**：从 `content_md` 取原文 → 生成例题 JSON + 误区 JSON**（含原文出处）** | 我 | 2 | 1–2 h |
| **4** | **人工逐条核**：每条都要能指到材料里的一行 | 我 | 3 | 40 min |
| **5** | **灌库**（走 `POST /api/review/import-edges` 的同类入口或新开一个） | 我 | 4 | 30 min |
| **6** | **前端确认**：面板显示出来 + 截图进 PPT | 我 | 5 | 15 min |
| **7** | **`templates.py` 的降级注释更新**（数据有了，注释还写着"表为空"） | 我 | 5 | 10 min |

**总计约 4–5 小时** —— **我能在今天内做完 1–6**（若你同意方案）。

---

## 六、⚠️ 我需要你拍板的一件事

**它的优先级 vs 其它**：

| 现在手上的事 | 谁 | 死线 |
|---|---|---|
| **导 LearnBuddy 对话记录** | **你** | 交付（9/26） |
| **验收 PPT v2** | **你** | 交付 |
| **录视频** | **P3** | 9/24 |
| **补例题/误区** | **我（待你同意）** | **9/24 功能冻结前** |

**⭐ 我的判断**：**这件事该做，但排在你那两件之后**。

**理由**：**例题/误区面板是"锦上添花"** ——
**它不在手册的必需交付物里**，也不在 A3-* 的硬指标里；
**而对话记录和 PPT 是必需的**。

**如果你说"先做它"**，我就开工（4–5 小时能完）；
**如果你说"排在后面"**，我就先待命，等你的两件完成。
