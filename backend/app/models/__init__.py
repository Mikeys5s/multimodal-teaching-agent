"""SQLAlchemy 数据模型（归属：P2）。

⚠️ 重要：新增模型文件后，**必须在本文件里 import 它**，
否则 Alembic 的 autogenerate 看不到该表，会生成空迁移。
"""

# 步骤 2 起在此逐个 import，例如：
# from app.models.material import Material, MaterialBlock  # noqa: F401
