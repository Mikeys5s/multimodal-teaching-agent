"""Pydantic 请求/响应模型（归属：P2）。

本层是**对外契约**：端点路径与字段结构在冻结后，**只允许新增，不允许改已有字段名或类型**
（SPEC §9.2 I-3）。字段名必须与 `docs/api-spec.md` 逐字一致 —— 前端照着它写解析代码。

按域拆分，一域一文件：

| 文件 | 覆盖的端点 | 对应规格 |
|---|---|---|
| `meta.py` | 健康检查、能力与限制（1–3） | api-spec §2 |
| `material.py` | 素材上传 / 清单 / 详情 / 块 / 大纲 / 题目（4–12） | api-spec §3 |
| `knowledge.py` | 知识点列表与详情（14、15） | api-spec §4.2 / §4.3 |
| `graph.py` | 图谱 / 学习路径 / 卡点回溯（16、17、28） | api-spec §4.4 |
| `report.py` | 质量报告 / 导出（18、19） | api-spec §4.5 / §4.6 |
| `qa.py` | 答疑会话 / 状态机 / SSE 事件（20–25） | api-spec §5 |
| `job.py` | 异步任务（26、27） | api-spec §6 |

契约层自检见 `tests/test_schemas.py`：它会断言每个响应模型都能渲染出 JSON Schema，
以及**关键字段名没有被改名**（改字段名是破坏性变更，会直接打断前端）。
"""
